### YamlMime:UniversalReference
items:
  - uid: office.OfficeExtension.TrackedObjects
    summary: '<span data-ttu-id="e1ef1-101">Коллекция отслеживаемых объектов, которая находится в контексте запроса.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-101">Collection of tracked objects, contained within a request context.</span></span> <span data-ttu-id="e1ef1-102">Дополнительные сведения см. в разделе "Context. Траккедобжектс".</span><span class="sxs-lookup"><span data-stu-id="e1ef1-102">See "context.trackedObjects" for more information.</span></span>'
    name: OfficeExtension.TrackedObjects
    fullName: OfficeExtension.TrackedObjects
    langs:
      - typeScript
    type: class
    package: office
    children:
      - office.OfficeExtension.TrackedObjects.add
      - office.OfficeExtension.TrackedObjects.add_1
      - office.OfficeExtension.TrackedObjects.remove
      - office.OfficeExtension.TrackedObjects.remove_1
  - uid: office.OfficeExtension.TrackedObjects.add
    summary: |-
      <span data-ttu-id="e1ef1-103">Отслеживание нового объекта для автоматической корректировки на основе окружающих изменений в документе.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-103">Track a new object for automatic adjustment based on surrounding changes in the document.</span></span> <span data-ttu-id="e1ef1-104">Это требуется только для некоторых типов объектов.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-104">Only some object types require this.</span></span> <span data-ttu-id="e1ef1-105">Если объект используется при вызове метода ". Sync" и за пределами последовательного выполнения пакета ". Run" и получения ошибки "Инвалидобжектпас" при задании свойства или вызове метода для объекта, необходимо добавить объект в коллекцию отслеживаемых объектов При первом создании объекта.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-105">If you are using an object across ".sync" calls and outside the sequential execution of a ".run" batch, and get an "InvalidObjectPath" error when setting a property or invoking a method on the object, you needed to have added the object to the tracked object collection when the object was first created.</span></span>

      <span data-ttu-id="e1ef1-106">Этот метод также имеет следующую сигнатуру:</span><span class="sxs-lookup"><span data-stu-id="e1ef1-106">This method also has the following signature:</span></span>

      <span data-ttu-id="e1ef1-107">`add(objects: ClientObject[]): void;`Где Objects — это массив объектов, которые необходимо отслеживать.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-107">`add(objects: ClientObject[]): void;` Where objects is an array of objects to be tracked.</span></span>
    name: add(object)
    fullName: add(object)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'add(object: ClientObject): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: object
          description: ''
          type:
            - office.OfficeExtension.ClientObject
  - uid: office.OfficeExtension.TrackedObjects.add_1
    summary: '<span data-ttu-id="e1ef1-108">Отслеживание набора объектов для автоматической корректировки на основе окружающих изменений в документе.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-108">Track a set of objects for automatic adjustment based on surrounding changes in the document.</span></span> <span data-ttu-id="e1ef1-109">Это требуется только для некоторых типов объектов.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-109">Only some object types require this.</span></span> <span data-ttu-id="e1ef1-110">Если объект используется при вызове метода ". Sync" и за пределами последовательного выполнения пакета ". Run" и получения ошибки "Инвалидобжектпас" при задании свойства или вызове метода для объекта, необходимо добавить объект в коллекцию отслеживаемых объектов При первом создании объекта.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-110">If you are using an object across ".sync" calls and outside the sequential execution of a ".run" batch, and get an "InvalidObjectPath" error when setting a property or invoking a method on the object, you needed to have added the object to the tracked object collection when the object was first created.</span></span>'
    name: add(objects)
    fullName: add(objects)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'add(objects: ClientObject[]): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: objects
          description: ''
          type:
            - 'ClientObject[]'
  - uid: office.OfficeExtension.TrackedObjects.remove
    summary: |-
      <span data-ttu-id="e1ef1-111">Освободите память, связанную с объектом, который ранее был добавлен в эту коллекцию.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-111">Release the memory associated with an object that was previously added to this collection.</span></span> <span data-ttu-id="e1ef1-112">Наличие большого количества отслеживаемых объектов замедляет ведущее приложение, поэтому не забывайте освобождать любые добавленные объекты после завершения их использования.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-112">Having many tracked objects slows down the host application, so please remember to free any objects you add, once you're done using them.</span></span> <span data-ttu-id="e1ef1-113">Вам потребуется позвонить `context.sync()` , прежде чем будет применен выпуск памяти.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-113">You will need to call `context.sync()` before the memory release takes effect.</span></span>

      <span data-ttu-id="e1ef1-114">Этот метод также имеет следующую сигнатуру:</span><span class="sxs-lookup"><span data-stu-id="e1ef1-114">This method also has the following signature:</span></span>

      <span data-ttu-id="e1ef1-115">`remove(objects: ClientObject[]): void;`Где Objects — это массив объектов, который требуется удалить.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-115">`remove(objects: ClientObject[]): void;` Where objects is an array of objects to be removed.</span></span>
    name: remove(object)
    fullName: remove(object)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'remove(object: ClientObject): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: object
          description: ''
          type:
            - office.OfficeExtension.ClientObject
  - uid: office.OfficeExtension.TrackedObjects.remove_1
    summary: '<span data-ttu-id="e1ef1-116">Освободите память, связанную с объектом, который ранее был добавлен в эту коллекцию.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-116">Release the memory associated with an object that was previously added to this collection.</span></span> <span data-ttu-id="e1ef1-117">Наличие большого количества отслеживаемых объектов замедляет ведущее приложение, поэтому не забывайте освобождать любые добавленные объекты после завершения их использования.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-117">Having many tracked objects slows down the host application, so please remember to free any objects you add, once you''re done using them.</span></span> <span data-ttu-id="e1ef1-118">Вам потребуется позвонить `context.sync()` , прежде чем будет применен выпуск памяти.</span><span class="sxs-lookup"><span data-stu-id="e1ef1-118">You will need to call `context.sync()` before the memory release takes effect.</span></span>'
    name: remove(objects)
    fullName: remove(objects)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'remove(objects: ClientObject[]): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: objects
          description: ''
          type:
            - 'ClientObject[]'