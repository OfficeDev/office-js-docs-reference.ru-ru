### YamlMime:UniversalReference
items:
  - uid: office.Office.Document
    summary: '<span data-ttu-id="5c500-101">Абстрактный класс, представляющий документ, с которым взаимодействует надстройка.</span><span class="sxs-lookup"><span data-stu-id="5c500-101">An abstract class that represents the document the add-in is interacting with.</span></span>'
    remarks: '<span data-ttu-id="5c500-102">**Узлы**: Access, Excel, PowerPoint, Project, Word</span><span class="sxs-lookup"><span data-stu-id="5c500-102">**Hosts**: Access, Excel, PowerPoint, Project, Word</span></span>'
    name: Office.Document
    fullName: Office.Document
    langs:
      - typeScript
    type: interface
    package: office
    children:
      - office.Office.Document.addHandlerAsync
      - office.Office.Document.addHandlerAsync_1
      - office.Office.Document.getActiveViewAsync
      - office.Office.Document.getActiveViewAsync_1
      - office.Office.Document.getFileAsync
      - office.Office.Document.getFileAsync_1
      - office.Office.Document.getFilePropertiesAsync
      - office.Office.Document.getFilePropertiesAsync_1
      - office.Office.Document.getMaxResourceIndexAsync
      - office.Office.Document.getMaxResourceIndexAsync_1
      - office.Office.Document.getMaxTaskIndexAsync
      - office.Office.Document.getMaxTaskIndexAsync_1
      - office.Office.Document.getProjectFieldAsync
      - office.Office.Document.getProjectFieldAsync_1
      - office.Office.Document.getResourceByIndexAsync
      - office.Office.Document.getResourceByIndexAsync_1
      - office.Office.Document.getResourceFieldAsync
      - office.Office.Document.getResourceFieldAsync_1
      - office.Office.Document.getSelectedDataAsync
      - office.Office.Document.getSelectedDataAsync_1
      - office.Office.Document.getSelectedResourceAsync
      - office.Office.Document.getSelectedResourceAsync_1
      - office.Office.Document.getSelectedTaskAsync
      - office.Office.Document.getSelectedTaskAsync_1
      - office.Office.Document.getSelectedViewAsync
      - office.Office.Document.getSelectedViewAsync_1
      - office.Office.Document.getTaskAsync
      - office.Office.Document.getTaskAsync_1
      - office.Office.Document.getTaskByIndexAsync
      - office.Office.Document.getTaskByIndexAsync_1
      - office.Office.Document.getTaskFieldAsync
      - office.Office.Document.getTaskFieldAsync_1
      - office.Office.Document.getWSSUrlAsync
      - office.Office.Document.getWSSUrlAsync_1
      - office.Office.Document.goToByIdAsync
      - office.Office.Document.goToByIdAsync_1
      - office.Office.Document.removeHandlerAsync
      - office.Office.Document.removeHandlerAsync_1
      - office.Office.Document.setResourceFieldAsync
      - office.Office.Document.setResourceFieldAsync_1
      - office.Office.Document.setSelectedDataAsync
      - office.Office.Document.setSelectedDataAsync_1
      - office.Office.Document.setTaskFieldAsync
      - office.Office.Document.setTaskFieldAsync_1
      - office.Office.Document.bindings
      - office.Office.Document.customXmlParts
      - office.Office.Document.mode
      - office.Office.Document.settings
      - office.Office.Document.url
  - uid: office.Office.Document.addHandlerAsync
    summary: <span data-ttu-id="5c500-103">Добавляет обработчик событий для события объекта Document.</span><span class="sxs-lookup"><span data-stu-id="5c500-103">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-104">**Набор требований**: [документевентс](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="5c500-104">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="5c500-105">Можно добавить несколько обработчиков событий для указанного объекта eventType, если имя каждой функции обработчика событий уникально.</span><span class="sxs-lookup"><span data-stu-id="5c500-105">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>
    name: 'addHandlerAsync(eventType, handler, options, callback)'
    fullName: 'addHandlerAsync(eventType, handler, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="5c500-106">Для события объекта Document параметру eventType может быть присвоено значение `Office.EventType.Document.SelectionChanged` или`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="5c500-106">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="5c500-107">или соответствующее текстовое значение этого перечисления.</span><span class="sxs-lookup"><span data-stu-id="5c500-107">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="5c500-108">Добавляемая функция обработчика событий, единственный параметр которой имеет тип [Office. свойство documentselectionchangedeventargs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="5c500-108">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="5c500-109">.</span><span class="sxs-lookup"><span data-stu-id="5c500-109"></span></span> <span data-ttu-id="5c500-110">Обязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-110">Required.</span></span>'
          type:
            - any
        - id: options
          description: '<span data-ttu-id="5c500-111">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-111">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-112">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-112">Optional.</span></span> <span data-ttu-id="5c500-113">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-113">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-114">.</span><span class="sxs-lookup"><span data-stu-id="5c500-114"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.addHandlerAsync_1
    summary: <span data-ttu-id="5c500-115">Добавляет обработчик событий для события объекта Document.</span><span class="sxs-lookup"><span data-stu-id="5c500-115">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-116">**Набор требований**: [документевентс](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="5c500-116">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="5c500-117">Можно добавить несколько обработчиков событий для указанного объекта eventType, если имя каждой функции обработчика событий уникально.</span><span class="sxs-lookup"><span data-stu-id="5c500-117">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>

      #### <a name="examples"></a><span data-ttu-id="5c500-118">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-118">Examples</span></span>

      ```javascript
      // The following example adds an event handler for the SelectionChanged event of a document
      function addSelectionChangedEventHandler() {
          Office.context.document.addHandlerAsync(Office.EventType.DocumentSelectionChanged, MyHandler);
      }

      function MyHandler(eventArgs) {
          write('Event raised: ' + eventArgs.type);
          doSomethingWithDocument(eventArgs.document);
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example adds a handler for the ResourceSelectionChanged event.
      // When the resource selection changes in the document, it gets the GUID of the selected resource.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
              });
          };

          // Get the GUID of the selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ResourceSelectionChanged
      // event handler in a Project add-in, see "Create your first task pane add-in
      // for Project 2013 by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example adds a handler for the TaskSelectionChanged event.
      // When the task selection changes in the document, it gets the GUID of the
      // selected task.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.TaskSelectionChanged,
                      getTaskGuid);
                  getTaskGuid();
              });
          };

          // Get the GUID of the selected task and display it in the add-in.
          function getTaskGuid() {
              Office.context.document.getSelectedTaskAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
      ```javascript
      // The following code example adds a handler for the ViewSelectionChanged
      // event. When the active view changes, it gets the name and type of the active view.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  getActiveView();
              });
          };

          // Get the name and type of the active view and display it in the add-in.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, result.value.viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For an example that shows how to use a ViewSelectionChanged event handler in a
      // Project add-in, see "Create your first task pane add-in for Project 2013 by
      // using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the ViewSelectionChanged event.
      // When the active view changes, the handler checks the view type. It enables a button if the view is a resource
      // view and disables the button if it isn't a resource view. Choosing the button gets the GUID of the selected
      // resource and displays it in the add-in.
      // The example assumes that your add-in has a reference to the jQuery library and that the following page controls
      // are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" disabled="disabled" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  // Add a ViewSelectionChanged event handler.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  $('#get-info').click(getResourceGuid);

                  // This example calls the handler on page load to get the active view
                  // of the default page.
                  getActiveView();
              });
          };

          // Activate the button based on the active view type of the document.
          // This is the ViewSelectionChanged event handler.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var viewType = result.value.viewType;
                          if (viewType == 6 ||   // ResourceForm
                              viewType == 7 ||   // ResourceSheet
                              viewType == 8 ||   // ResourceGraph
                              viewType == 15) {  // ResourceUsage
                              $('#get-info').removeAttr('disabled');
                          }
                          else {
                              $('#get-info').attr('disabled', 'disabled');
                          }
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ViewSelectionChanged event handler in a Project add-in,
      // see "Create your first task pane add-in for Project by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
    name: 'addHandlerAsync(eventType, handler, callback)'
    fullName: 'addHandlerAsync(eventType, handler, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="5c500-119">Для события объекта Document параметру eventType может быть присвоено значение `Office.EventType.Document.SelectionChanged` или`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="5c500-119">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="5c500-120">или соответствующее текстовое значение этого перечисления.</span><span class="sxs-lookup"><span data-stu-id="5c500-120">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="5c500-121">Добавляемая функция обработчика событий, единственный параметр которой имеет тип [Office. свойство documentselectionchangedeventargs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="5c500-121">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="5c500-122">.</span><span class="sxs-lookup"><span data-stu-id="5c500-122"></span></span> <span data-ttu-id="5c500-123">Обязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-123">Required.</span></span>'
          type:
            - any
        - id: callback
          description: '<span data-ttu-id="5c500-124">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-124">Optional.</span></span> <span data-ttu-id="5c500-125">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-125">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-126">.</span><span class="sxs-lookup"><span data-stu-id="5c500-126"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.getActiveViewAsync
    summary: <span data-ttu-id="5c500-127">Возвращает состояние текущего представления презентации (редактирование или чтение).</span><span class="sxs-lookup"><span data-stu-id="5c500-127">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="5c500-128">**Набор требований**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="5c500-128">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="5c500-129">Может активировать событие при изменении представления.</span><span class="sxs-lookup"><span data-stu-id="5c500-129">Can trigger an event when the view changes.</span></span>
    name: 'getActiveViewAsync(options, callback)'
    fullName: 'getActiveViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-130">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-130">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-131">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-131">Optional.</span></span> <span data-ttu-id="5c500-132">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-132">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-133">.</span><span class="sxs-lookup"><span data-stu-id="5c500-133"></span></span> <span data-ttu-id="5c500-134">`value` Свойство Result является состоянием текущего представления презентации.</span><span class="sxs-lookup"><span data-stu-id="5c500-134">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="5c500-135">Возвращаемое значение может быть "Edit" или "Read".</span><span class="sxs-lookup"><span data-stu-id="5c500-135">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="5c500-136">"Изменить" соответствует любому из представлений, в котором можно редактировать слайды, такие как обычный режим или режим структуры.</span><span class="sxs-lookup"><span data-stu-id="5c500-136">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="5c500-137">"Read" соответствует либо режиму показа слайдов, либо режиму чтения.</span><span class="sxs-lookup"><span data-stu-id="5c500-137">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getActiveViewAsync_1
    summary: <span data-ttu-id="5c500-138">Возвращает состояние текущего представления презентации (редактирование или чтение).</span><span class="sxs-lookup"><span data-stu-id="5c500-138">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="5c500-139">**Набор требований**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="5c500-139">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="5c500-140">Может активировать событие при изменении представления.</span><span class="sxs-lookup"><span data-stu-id="5c500-140">Can trigger an event when the view changes.</span></span>

      #### <a name="examples"></a><span data-ttu-id="5c500-141">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-141">Examples</span></span>

      ```javascript
      function getFileView() {
          // Get whether the current view is edit or read.
          Office.context.document.getActiveViewAsync(function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage(asyncResult.value);
              }
          });
      }
      ```
    name: getActiveViewAsync(callback)
    fullName: getActiveViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-142">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-142">Optional.</span></span> <span data-ttu-id="5c500-143">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-143">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-144">.</span><span class="sxs-lookup"><span data-stu-id="5c500-144"></span></span> <span data-ttu-id="5c500-145">`value` Свойство Result является состоянием текущего представления презентации.</span><span class="sxs-lookup"><span data-stu-id="5c500-145">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="5c500-146">Возвращаемое значение может быть "Edit" или "Read".</span><span class="sxs-lookup"><span data-stu-id="5c500-146">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="5c500-147">"Изменить" соответствует любому из представлений, в котором можно редактировать слайды, такие как обычный режим или режим структуры.</span><span class="sxs-lookup"><span data-stu-id="5c500-147">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="5c500-148">"Read" соответствует либо режиму показа слайдов, либо режиму чтения.</span><span class="sxs-lookup"><span data-stu-id="5c500-148">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getFileAsync
    summary: "<span data-ttu-id=\"5c500-149\">Возвращает полный файл документа по фрагментам размером до 4194304\_байт (4\_МБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-149\">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id=\"5c500-150\">Надстройки для iOS поддерживают фрагменты до 65536\_байт (64\_КБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-150\">For add-ins for iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id=\"5c500-151\">Обратите внимание, что если указать размер фрагмента выше допустимого, возникнет сбой \"Внутренняя ошибка\".</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-151\">Note that specifying file slice size of above permitted limit will result in an \"Internal Error\" failure.</span></span>"
    remarks: "<span data-ttu-id=\"5c500-152\">**Наборы**обязательных элементов: [компресседфиле](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (при использовании`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-152\">**Requirement sets**: [CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-153\">), [Файл](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-153\">), [File](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span></span><!-- --><span data-ttu-id=\"5c500-154\">, [Текстфиле](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (при использовании`Office.FileType.Text`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-154\">, [TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id=\"5c500-155\">)</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-155\"></span></span>\n\n<span data-ttu-id=\"5c500-156\">Для надстроек, работающих в ведущих приложениях Office, отличных от Office для iOS, метод getFileAsync поддерживает извлечение файлов в секторах размером до 4194304 байт (4 МБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-156\">For add-ins running in Office host applications other than Office for iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id=\"5c500-157\">Для надстроек, работающих в приложениях Office для iOS, метод getFileAsync поддерживает получение файлов по фрагментам размером до 65536\_байтов (64\_КБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-157\">For add-ins running in Office for iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>\n\n<span data-ttu-id=\"5c500-158\">Параметр fileType можно указать с помощью перечисления [Office. filetype](xref:office.Office.FileType) или текстовых значений.</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-158\">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id=\"5c500-159\">Но возможные значения могут зависеть от ведущего приложения:</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-159\">But the possible values vary with the host:</span></span>\n\n<span data-ttu-id=\"5c500-160\">Excel для настольных систем Windows, iPad и Excel Online:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-160\">Excel for Windows desktop, iPad, and Excel Online: `Office.FileType.Compressed`</span></span>\n\n<span data-ttu-id=\"5c500-161\">Excel для Mac:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-161\">Excel for Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-162\">, `Office.FileType.Pdf`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-162\"></span></span>\n\n<span data-ttu-id=\"5c500-163\">PowerPoint для Windows для настольных ПК, Mac, iPad и PowerPoint Online:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-163\">PowerPoint for Windows desktop, Mac, iPad, and PowerPoint Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-164\">, `Office.FileType.Pdf`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-164\"></span></span>\n\n<span data-ttu-id=\"5c500-165\">Word для Windows для настольных ПК, Mac, iPad и Word Online:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-165\">Word for Windows desktop, Mac, iPad, and Word Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-166\">, `Office.FileType.Pdf`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-166\"></span></span><!-- --><span data-ttu-id=\"5c500-167\">, `Office.FileType.Text`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-167\"></span></span>\n\n#### <a name=\"examples\"></a><span data-ttu-id=\"5c500-168\">Примеры</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-168\">Examples</span></span>\n\n```javascript\n// The following example gets the document in Office Open XML (\"compressed\") format in 65536 bytes (64 KB) slices.\n// Note: The implementation of app.showNotification in this example is from the Visual Studio template for Office Add-ins.\nfunction getDocumentAsCompressed() {\n    Office.context.document.getFileAsync(Office.FileType.Compressed, { sliceSize: 65536 /*64 KB*/ }, \n        function (result) {\n            if (result.status == \"succeeded\") {\n            // If the getFileAsync call succeeded, then\n            // result.value will return a valid File Object.\n            var myFile = result.value;\n            var sliceCount = myFile.sliceCount;\n            var slicesReceived = 0, gotAllSlices = true, docdataSlices = [];\n            app.showNotification(\"File size:\" + myFile.size + \" #Slices: \" + sliceCount);\n\n            // Get the file slices.\n            getSliceAsync(myFile, 0, sliceCount, gotAllSlices, docdataSlices, slicesReceived);\n            }\n            else {\n            app.showNotification(\"Error:\", result.error.message);\n            }\n    });\n}\n\nfunction getSliceAsync(file, nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived) {\n    file.getSliceAsync(nextSlice, function (sliceResult) {\n        if (sliceResult.status == \"succeeded\") {\n            if (!gotAllSlices) { // Failed to get all slices, no need to continue.\n                return;\n            }\n\n            // Got one slice, store it in a temporary array.\n            // (Or you can do something else, such as\n            // send it to a third-party server.)\n            docdataSlices[sliceResult.value.index] = sliceResult.value.data;\n            if (++slicesReceived == sliceCount) {\n              // All slices have been received.\n              file.closeAsync();\n              onGotAllSlices(docdataSlices);\n            }\n            else {\n                getSliceAsync(file, ++nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived);\n            }\n        }\n            else {\n                gotAllSlices = false;\n                file.closeAsync();\n                app.showNotification(\"getSliceAsync Error:\", sliceResult.error.message);\n            }\n    });\n}\n\nfunction onGotAllSlices(docdataSlices) {\n    var docdata = [];\n    for (var i = 0; i < docdataSlices.length; i++) {\n        docdata = docdata.concat(docdataSlices[i]);\n    }\n\n    var fileContent = new String();\n    for (var j = 0; j < docdata.length; j++) {\n        fileContent += String.fromCharCode(docdata[j]);\n    }\n\n    // Now all the file content is stored in 'fileContent' variable,\n    // you can do something with it, such as print, fax...\n}\n\n// The following example gets the document in PDF format.\nOffice.context.document.getFileAsync(Office.FileType.Pdf,\n    function(result) {\n        if (result.status == \"succeeded\") {\n            var myFile = result.value;\n            var sliceCount = myFile.sliceCount;\n            app.showNotification(\"File size:\" + myFile.size + \" #Slices: \" + sliceCount);\n            // Now, you can call getSliceAsync to download the files,\n            // as described in the previous code segment (compressed format).\n            \n            myFile.closeAsync();\n        }\n        else {\n            app.showNotification(\"Error:\", result.error.message);\n        }\n}\n);\n```"
    name: 'getFileAsync(fileType, options, callback)'
    fullName: 'getFileAsync(fileType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, options?: GetFileOptions, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: '<span data-ttu-id="5c500-169">Формат, в котором возвращается файл</span><span class="sxs-lookup"><span data-stu-id="5c500-169">The format in which the file will be returned</span></span>'
          type:
            - office.Office.FileType
        - id: options
          description: '<span data-ttu-id="5c500-170">Предоставляет параметры для задания размера срезов, на которые будет разбит документ.</span><span class="sxs-lookup"><span data-stu-id="5c500-170">Provides options for setting the size of slices that the document will be divided into.</span></span>'
          type:
            - office.Office.GetFileOptions
        - id: callback
          description: '<span data-ttu-id="5c500-171">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-171">Optional.</span></span> <span data-ttu-id="5c500-172">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-172">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-173">.</span><span class="sxs-lookup"><span data-stu-id="5c500-173"></span></span> <span data-ttu-id="5c500-174">Результатом `value` этого свойства является объект File.</span><span class="sxs-lookup"><span data-stu-id="5c500-174">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFileAsync_1
    summary: "<span data-ttu-id=\"5c500-175\">Возвращает полный файл документа по фрагментам размером до 4194304\_байт (4\_МБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-175\">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id=\"5c500-176\">Надстройки для iOS поддерживают фрагменты до 65536\_байт (64\_КБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-176\">For add-ins for iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id=\"5c500-177\">Обратите внимание, что если указать размер фрагмента выше допустимого, возникнет сбой \"Внутренняя ошибка\".</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-177\">Note that specifying file slice size of above permitted limit will result in an \"Internal Error\" failure.</span></span>"
    remarks: "<span data-ttu-id=\"5c500-178\">**Наборы**обязательных элементов: [компресседфиле](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (при использовании`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-178\">**Requirement sets**: [CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-179\">), [Файл](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-179\">), [File](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span></span><!-- --><span data-ttu-id=\"5c500-180\">, [Текстфиле](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (при использовании`Office.FileType.Text`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-180\">, [TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id=\"5c500-181\">)</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-181\"></span></span>\n\n<span data-ttu-id=\"5c500-182\">Для надстроек, работающих в ведущих приложениях Office, отличных от Office для iOS, метод getFileAsync поддерживает извлечение файлов в секторах размером до 4194304 байт (4 МБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-182\">For add-ins running in Office host applications other than Office for iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id=\"5c500-183\">Для надстроек, работающих в приложениях Office для iOS, метод getFileAsync поддерживает получение файлов по фрагментам размером до 65536\_байтов (64\_КБ).</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-183\">For add-ins running in Office for iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>\n\n<span data-ttu-id=\"5c500-184\">Параметр fileType можно указать с помощью перечисления [Office. filetype](xref:office.Office.FileType) или текстовых значений.</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-184\">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id=\"5c500-185\">Но возможные значения могут зависеть от ведущего приложения:</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-185\">But the possible values vary with the host:</span></span>\n\n<span data-ttu-id=\"5c500-186\">Excel для настольных систем Windows, iPad и Excel Online:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-186\">Excel for Windows desktop, iPad, and Excel Online: `Office.FileType.Compressed`</span></span>\n\n<span data-ttu-id=\"5c500-187\">Excel для Mac:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-187\">Excel for Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-188\">, `Office.FileType.Pdf`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-188\"></span></span>\n\n<span data-ttu-id=\"5c500-189\">PowerPoint для Windows для настольных ПК, Mac, iPad и PowerPoint Online:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-189\">PowerPoint for Windows desktop, Mac, iPad, and PowerPoint Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-190\">, `Office.FileType.Pdf`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-190\"></span></span>\n\n<span data-ttu-id=\"5c500-191\">Word для Windows для настольных ПК, Mac, iPad и Word Online:`Office.FileType.Compressed`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-191\">Word for Windows desktop, Mac, iPad, and Word Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id=\"5c500-192\">, `Office.FileType.Pdf`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-192\"></span></span><!-- --><span data-ttu-id=\"5c500-193\">, `Office.FileType.Text`</span><span class=\"sxs-lookup\"><span data-stu-id=\"5c500-193\"></span></span>"
    name: 'getFileAsync(fileType, callback)'
    fullName: 'getFileAsync(fileType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: '<span data-ttu-id="5c500-194">Формат, в котором возвращается файл</span><span class="sxs-lookup"><span data-stu-id="5c500-194">The format in which the file will be returned</span></span>'
          type:
            - office.Office.FileType
        - id: callback
          description: '<span data-ttu-id="5c500-195">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-195">Optional.</span></span> <span data-ttu-id="5c500-196">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-196">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-197">.</span><span class="sxs-lookup"><span data-stu-id="5c500-197"></span></span> <span data-ttu-id="5c500-198">Результатом `value` этого свойства является объект File.</span><span class="sxs-lookup"><span data-stu-id="5c500-198">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync
    summary: <span data-ttu-id="5c500-199">Получает свойства текущего документа.</span><span class="sxs-lookup"><span data-stu-id="5c500-199">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-200">**Наборы**обязательных элементов: [не в наборе](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="5c500-200">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="5c500-201">URL-адрес файла возвращается с помощью свойства URL`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="5c500-201">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="5c500-202">.</span><span class="sxs-lookup"><span data-stu-id="5c500-202"></span></span>
    name: 'getFilePropertiesAsync(options, callback)'
    fullName: 'getFilePropertiesAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-203">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-203">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-204">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-204">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-205">.</span><span class="sxs-lookup"><span data-stu-id="5c500-205"></span></span> <span data-ttu-id="5c500-206">Результатом `value` этого свойства является свойство файла (URL-адрес которого находится по адресу`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="5c500-206">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="5c500-207">).</span><span class="sxs-lookup"><span data-stu-id="5c500-207"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync_1
    summary: <span data-ttu-id="5c500-208">Получает свойства текущего документа.</span><span class="sxs-lookup"><span data-stu-id="5c500-208">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-209">**Наборы**обязательных элементов: [не в наборе](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="5c500-209">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="5c500-210">URL-адрес файла возвращается с помощью свойства URL`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="5c500-210">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="5c500-211">.</span><span class="sxs-lookup"><span data-stu-id="5c500-211"></span></span>

      #### <a name="examples"></a><span data-ttu-id="5c500-212">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-212">Examples</span></span>

      ```javascript
      // To read the URL of the current file, you need to write a callback function that returns the URL.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that returns the value of the file's URL
      //    to the callback parameter of the getFilePropertiesAsync method.
      // 2. Display the value on the add-in's page.
      function getFileUrl() {
          // Get the URL of the current file.
          Office.context.document.getFilePropertiesAsync(function (asyncResult) {
              var fileUrl = asyncResult.value.url;
              if (fileUrl == "") {
                  showMessage("The file hasn't been saved yet. Save the file and try again");
              }
              else {
                  showMessage(fileUrl);
              }
          });
      }
      ```
    name: getFilePropertiesAsync(callback)
    fullName: getFilePropertiesAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-213">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-213">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-214">.</span><span class="sxs-lookup"><span data-stu-id="5c500-214"></span></span> <span data-ttu-id="5c500-215">Результатом `value` этого свойства является свойство файла (URL-адрес которого находится по адресу`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="5c500-215">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="5c500-216">).</span><span class="sxs-lookup"><span data-stu-id="5c500-216"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync
    summary: |-
      <span data-ttu-id="5c500-217">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-217">Project documents only.</span></span> <span data-ttu-id="5c500-218">Получение максимального индекса коллекции ресурсов в текущем проекте.</span><span class="sxs-lookup"><span data-stu-id="5c500-218">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="5c500-219">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-219">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxResourceIndexAsync(options, callback)'
    fullName: 'getMaxResourceIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-220">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-220">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-221">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-221">Optional.</span></span> <span data-ttu-id="5c500-222">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-222">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-223">.</span><span class="sxs-lookup"><span data-stu-id="5c500-223"></span></span> <span data-ttu-id="5c500-224">`value` Свойство Result — это максимальный номер индекса в коллекции ресурсов текущего проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-224">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync_1
    summary: |-
      <span data-ttu-id="5c500-225">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-225">Project documents only.</span></span> <span data-ttu-id="5c500-226">Получение максимального индекса коллекции ресурсов в текущем проекте.</span><span class="sxs-lookup"><span data-stu-id="5c500-226">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="5c500-227">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-227">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxResourceIndexAsync(callback)
    fullName: getMaxResourceIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-228">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-228">Examples</span></span>

          ```javascript
          // The following code example calls getResourceTaskIndexAsync to get the maximum index of the collection 
          // of resources in the current project. Then it uses the returned value and the getResourceByIndexAsync
          // method to get each resource GUID. The example assumes that your add-in has a reference to the 
          // jQuery library and that the following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-229">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-229">Optional.</span></span> <span data-ttu-id="5c500-230">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-230">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-231">.</span><span class="sxs-lookup"><span data-stu-id="5c500-231"></span></span> <span data-ttu-id="5c500-232">`value` Свойство Result — это максимальный номер индекса в коллекции ресурсов текущего проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-232">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync
    summary: |-
      <span data-ttu-id="5c500-233">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-233">Project documents only.</span></span> <span data-ttu-id="5c500-234">Получение максимального индекса коллекции задач в текущем проекте.</span><span class="sxs-lookup"><span data-stu-id="5c500-234">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="5c500-235">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-235">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxTaskIndexAsync(options, callback)'
    fullName: 'getMaxTaskIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-236">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-236">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-237">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-237">Optional.</span></span> <span data-ttu-id="5c500-238">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-238">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-239">.</span><span class="sxs-lookup"><span data-stu-id="5c500-239"></span></span> <span data-ttu-id="5c500-240">`value` Свойство Result — это наибольший номер индекса в коллекции задач текущего проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-240">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync_1
    summary: |-
      <span data-ttu-id="5c500-241">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-241">Project documents only.</span></span> <span data-ttu-id="5c500-242">Получение максимального индекса коллекции задач в текущем проекте.</span><span class="sxs-lookup"><span data-stu-id="5c500-242">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="5c500-243">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-243">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxTaskIndexAsync(callback)
    fullName: getMaxTaskIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-244">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-244">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the maximum index
          // of the collection of tasks in the current project. Then it uses the returned value
          // with the getTaskByIndexAsync method to get each task GUID.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-245">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-245">Optional.</span></span> <span data-ttu-id="5c500-246">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-246">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-247">.</span><span class="sxs-lookup"><span data-stu-id="5c500-247"></span></span> <span data-ttu-id="5c500-248">`value` Свойство Result — это наибольший номер индекса в коллекции задач текущего проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-248">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getProjectFieldAsync
    summary: '<span data-ttu-id="5c500-249">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-249">Project documents only.</span></span> <span data-ttu-id="5c500-250">Получение поля проекта (например,</span><span class="sxs-lookup"><span data-stu-id="5c500-250">Get Project field (Ex.</span></span> <span data-ttu-id="5c500-251">Прожектвебакцессурл).</span><span class="sxs-lookup"><span data-stu-id="5c500-251">ProjectWebAccessURL).</span></span>'
    name: 'getProjectFieldAsync(fieldId, options, callback)'
    fullName: 'getProjectFieldAsync(fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fieldId
          description: <span data-ttu-id="5c500-252">Поля уровня проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-252">Project level fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="5c500-253">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-253">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-254">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-254">Optional.</span></span> <span data-ttu-id="5c500-255">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-255">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-256">.</span><span class="sxs-lookup"><span data-stu-id="5c500-256"></span></span> <span data-ttu-id="5c500-257">`value` Свойство Result содержит `fieldValue` свойство, которое представляет значение указанного поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-257">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getProjectFieldAsync_1
    summary: '<span data-ttu-id="5c500-258">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-258">Project documents only.</span></span> <span data-ttu-id="5c500-259">Получение поля проекта (например,</span><span class="sxs-lookup"><span data-stu-id="5c500-259">Get Project field (Ex.</span></span> <span data-ttu-id="5c500-260">Прожектвебакцессурл).</span><span class="sxs-lookup"><span data-stu-id="5c500-260">ProjectWebAccessURL).</span></span>'
    name: 'getProjectFieldAsync(fieldId, callback)'
    fullName: 'getProjectFieldAsync(fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-261">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-261">Examples</span></span>

          ```javascript
          // The following code example gets the values of three specified fields for the active project, 
          // and then displays the values in the add-in.
          // The example calls getProjectFieldAsync recursively, after the previous call returns successfully.
          // It also tracks the calls to determine when all calls are sent.
          // The example assumes your add-in has a reference to the jQuery library and that the 
          // following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information for the active project.
                      getProjectInformation();
                  });
              };

              // Get the specified fields for the active project.
              function getProjectInformation() {
                  var fields =
                      [Office.ProjectProjectFields.Start, 
                       Office.ProjectProjectFields.Finish, 
                       Office.ProjectProjectFields.GUID];
                  var fieldValues = ['Start: ', 'Finish: ', 'GUID: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == fields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }
                      else {
                          Office.context.document.getProjectFieldAsync(
                              fields[index],
                              function (result) {

                                  // If the call is successful, get the field value and then get the next field.
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: fieldId
          description: <span data-ttu-id="5c500-262">Поля уровня проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-262">Project level fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="5c500-263">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-263">Optional.</span></span> <span data-ttu-id="5c500-264">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-264">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-265">.</span><span class="sxs-lookup"><span data-stu-id="5c500-265"></span></span> <span data-ttu-id="5c500-266">`value` Свойство Result содержит `fieldValue` свойство, которое представляет значение указанного поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-266">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync
    summary: |-
      <span data-ttu-id="5c500-267">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-267">Project documents only.</span></span> <span data-ttu-id="5c500-268">Получение GUID ресурса с указанным индексом в коллекции ресурсов.</span><span class="sxs-lookup"><span data-stu-id="5c500-268">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="5c500-269">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-269">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="5c500-270">Индекс ресурса в коллекции ресурсов для проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-270">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="5c500-271">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-271">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-272">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-272">Optional.</span></span> <span data-ttu-id="5c500-273">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-273">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-274">.</span><span class="sxs-lookup"><span data-stu-id="5c500-274"></span></span> <span data-ttu-id="5c500-275">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-275">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync_1
    summary: |-
      <span data-ttu-id="5c500-276">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-276">Project documents only.</span></span> <span data-ttu-id="5c500-277">Получение GUID ресурса с указанным индексом в коллекции ресурсов.</span><span class="sxs-lookup"><span data-stu-id="5c500-277">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="5c500-278">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-278">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-279">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-279">Examples</span></span>

          ```javascript
          // The following code example calls getMaxResourceIndexAsync to get the maximum index in the project's resource
          // collection, and then calls getResourceByIndexAsync to get the GUID for each resource.
          // The example assumes that your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="5c500-280">Индекс ресурса в коллекции ресурсов для проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-280">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="5c500-281">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-281">Optional.</span></span> <span data-ttu-id="5c500-282">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-282">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-283">.</span><span class="sxs-lookup"><span data-stu-id="5c500-283"></span></span> <span data-ttu-id="5c500-284">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-284">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync
    summary: <span data-ttu-id="5c500-285">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-285">Project documents only.</span></span> <span data-ttu-id="5c500-286">Получение поля ресурса для указанного идентификатора ресурса. (ex. ResourceName)</span><span class="sxs-lookup"><span data-stu-id="5c500-286">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="5c500-287">Строка или значение идентификатора ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-287">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-288">Поля ресурсов.</span><span class="sxs-lookup"><span data-stu-id="5c500-288">Resource Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="5c500-289">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-289">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-290">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-290">Optional.</span></span> <span data-ttu-id="5c500-291">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-291">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-292">.</span><span class="sxs-lookup"><span data-stu-id="5c500-292"></span></span> <span data-ttu-id="5c500-293">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-293">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync_1
    summary: <span data-ttu-id="5c500-294">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-294">Project documents only.</span></span> <span data-ttu-id="5c500-295">Получение поля ресурса для указанного идентификатора ресурса. (ex. ResourceName)</span><span class="sxs-lookup"><span data-stu-id="5c500-295">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-296">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-296">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource
          // that's currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="5c500-297">Строка или значение идентификатора ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-297">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-298">Поля ресурсов.</span><span class="sxs-lookup"><span data-stu-id="5c500-298">Resource Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="5c500-299">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-299">Optional.</span></span> <span data-ttu-id="5c500-300">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-300">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-301">.</span><span class="sxs-lookup"><span data-stu-id="5c500-301"></span></span> <span data-ttu-id="5c500-302">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-302">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedDataAsync
    summary: <span data-ttu-id="5c500-303">Считывает данные в текущем выделенном фрагменте документа.</span><span class="sxs-lookup"><span data-stu-id="5c500-303">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-304">**Наборы**обязательных элементов: [хтмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (при использовании`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="5c500-304">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="5c500-305">), [МатрикскоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (при использовании`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="5c500-305">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="5c500-306">), [УксмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (при использовании`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="5c500-306">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="5c500-307">), [Выделение](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="5c500-307">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="5c500-308">, [ТаблекоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (при использовании`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="5c500-308">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="5c500-309">), [ТексткоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (при использовании`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="5c500-309">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="5c500-310">)</span><span class="sxs-lookup"><span data-stu-id="5c500-310"></span></span>

      <span data-ttu-id="5c500-311">В функции обратного вызова, которая передается в метод getSelectedDataAsync, можно использовать свойства объекта AsyncResult, чтобы получить следующие сведения.</span><span class="sxs-lookup"><span data-stu-id="5c500-311">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="5c500-312">Свойство</span><span class="sxs-lookup"><span data-stu-id="5c500-312">Property</span></span></th> <th><span data-ttu-id="5c500-313">Назначение</span><span class="sxs-lookup"><span data-stu-id="5c500-313">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="5c500-314">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="5c500-314">AsyncResult.value</span></span></td> <td><span data-ttu-id="5c500-315">Всегда возвращает значение undefined, так как нет объекта или данных для извлечения.</span><span class="sxs-lookup"><span data-stu-id="5c500-315">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-316">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="5c500-316">AsyncResult.status</span></span></td> <td><span data-ttu-id="5c500-317">Определяет, удалось ли выполнить операцию.</span><span class="sxs-lookup"><span data-stu-id="5c500-317">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-318">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="5c500-318">AsyncResult.error</span></span></td> <td><span data-ttu-id="5c500-319">Доступ к объекту Error, который предоставляет сведения об ошибке, если операция завершилась неудачно.</span><span class="sxs-lookup"><span data-stu-id="5c500-319">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-320">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="5c500-320">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="5c500-321">Определяемый пользователем элемент любого типа, который возвращается в объекте AsyncResult без изменения.</span><span class="sxs-lookup"><span data-stu-id="5c500-321">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="5c500-322">Допустимые значения параметра [Office. CoercionType](xref:office.Office.CoercionType) зависят от узла.</span><span class="sxs-lookup"><span data-stu-id="5c500-322">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="5c500-323">Host (Узел)</span><span class="sxs-lookup"><span data-stu-id="5c500-323">Host</span></span></th> <th><span data-ttu-id="5c500-324">Поддерживаемое значение coercionType</span><span class="sxs-lookup"><span data-stu-id="5c500-324">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="5c500-325">Excel, PowerPoint, Project и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-325">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="5c500-326">`Office.CoercionType.Text`substring</span><span class="sxs-lookup"><span data-stu-id="5c500-326">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-327">Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-327">Excel and Word</span></span></td> <td><span data-ttu-id="5c500-328">`Office.CoercionType.Matrix`(массив массивов)</span><span class="sxs-lookup"><span data-stu-id="5c500-328">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-329">Access, Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-329">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="5c500-330">`Office.CoercionType.Table`(Объект TableData)</span><span class="sxs-lookup"><span data-stu-id="5c500-330">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-331">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-331">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="5c500-332">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-332">Word</span></span></td> <td><span data-ttu-id="5c500-333">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="5c500-333">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-334">PowerPoint и PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="5c500-334">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="5c500-335">Excel, PowerPoint и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-335">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="5c500-336">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-336">Examples</span></span>

      ```javascript
      // The following example uses the getSelectedDataAsync method of the Document object to retrieve the
      // user's current selection as text, and then display it in the add-in's page.

      // Display the user's current selection.
      function showSelection() {
          Office.context.document.getSelectedDataAsync(
              "text",                        // coercionType
              {valueFormat: "unformatted",   // valueFormat
              filterType: "all"},            // filterType
              function (result) {            // callback
                  var dataValue; 
                  dataValue = result.value;
                  write('Selected data is: ' + dataValue);
              });
      }
      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // To read the value of the current selection, you need to write a callback function that reads the selection.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that reads the value of the current selection
      //    to the callback parameter of the getSelectedDataAsync method.
      // 2. Read the selection as text, unformatted, and not filtered.
      // 3. Display the value on the add-in's page.
      function getText() {
          Office.context.document.getSelectedDataAsync(Office.CoercionType.Text, 
              { valueFormat: "unformatted", filterType: "all" },
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  } 
                  else {
                      // Get selected data.
                      var dataValue = asyncResult.value; 
                      write('Selected data is ' + dataValue);
                  }            
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example gets the values of the selected cells. It uses the optional
      // asyncContext parameter to pass some text to the callback function.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page controls are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  $('#get-info').click(getSelectedText);
              });
          };

          // Get the text from the selected cells in the document, and display it in the add-in.
          function getSelectedText() {
              Office.context.document.getSelectedDataAsync(
                  Office.CoercionType.Text,
                  {asyncContext: 'Some related info'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'Selected text: {0}<br/>Passed info: {1}',
                              result.value, result.asyncContext);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'getSelectedDataAsync(coercionType, options, callback)'
    fullName: 'getSelectedDataAsync(coercionType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, options?: GetSelectedDataOptions, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="5c500-337">Тип возвращаемой структуры данных.</span><span class="sxs-lookup"><span data-stu-id="5c500-337">The type of data structure to return.</span></span> <span data-ttu-id="5c500-338">В разделе "Примечания" представлены поддерживаемые типы приведения для каждого узла.</span><span class="sxs-lookup"><span data-stu-id="5c500-338">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - office.Office.CoercionType
        - id: options
          description: <span data-ttu-id="5c500-339">Предоставляет параметры для настройки возвращаемых данных и их форматирования.</span><span class="sxs-lookup"><span data-stu-id="5c500-339">Provides options for customizing what data is returned and how it is formatted.</span></span>
          type:
            - office.Office.GetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="5c500-340">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-340">Optional.</span></span> <span data-ttu-id="5c500-341">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-341">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-342">.</span><span class="sxs-lookup"><span data-stu-id="5c500-342"></span></span> <span data-ttu-id="5c500-343">`value` Свойство Result представляет данные в текущем выделенном фрагменте.</span><span class="sxs-lookup"><span data-stu-id="5c500-343">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="5c500-344">Он возвращается в структуре данных или формате, указанном с помощью параметра coercionType.</span><span class="sxs-lookup"><span data-stu-id="5c500-344">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="5c500-345">(Дополнительные сведения о приведении типов данных см. в разделе Замечания.)</span><span class="sxs-lookup"><span data-stu-id="5c500-345">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedDataAsync_1
    summary: <span data-ttu-id="5c500-346">Считывает данные в текущем выделенном фрагменте документа.</span><span class="sxs-lookup"><span data-stu-id="5c500-346">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-347">**Наборы**обязательных элементов: [хтмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (при использовании`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="5c500-347">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="5c500-348">), [МатрикскоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (при использовании`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="5c500-348">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="5c500-349">), [УксмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (при использовании`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="5c500-349">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="5c500-350">), [Выделение](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="5c500-350">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="5c500-351">, [ТаблекоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (при использовании`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="5c500-351">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="5c500-352">), [ТексткоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (при использовании`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="5c500-352">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="5c500-353">)</span><span class="sxs-lookup"><span data-stu-id="5c500-353"></span></span>

      <span data-ttu-id="5c500-354">В функции обратного вызова, которая передается в метод getSelectedDataAsync, можно использовать свойства объекта AsyncResult, чтобы получить следующие сведения.</span><span class="sxs-lookup"><span data-stu-id="5c500-354">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="5c500-355">Свойство</span><span class="sxs-lookup"><span data-stu-id="5c500-355">Property</span></span></th> <th><span data-ttu-id="5c500-356">Назначение</span><span class="sxs-lookup"><span data-stu-id="5c500-356">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="5c500-357">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="5c500-357">AsyncResult.value</span></span></td> <td><span data-ttu-id="5c500-358">Всегда возвращает значение undefined, так как нет объекта или данных для извлечения.</span><span class="sxs-lookup"><span data-stu-id="5c500-358">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-359">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="5c500-359">AsyncResult.status</span></span></td> <td><span data-ttu-id="5c500-360">Определяет, удалось ли выполнить операцию.</span><span class="sxs-lookup"><span data-stu-id="5c500-360">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-361">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="5c500-361">AsyncResult.error</span></span></td> <td><span data-ttu-id="5c500-362">Доступ к объекту Error, который предоставляет сведения об ошибке, если операция завершилась неудачно.</span><span class="sxs-lookup"><span data-stu-id="5c500-362">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-363">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="5c500-363">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="5c500-364">Определяемый пользователем элемент любого типа, который возвращается в объекте AsyncResult без изменения.</span><span class="sxs-lookup"><span data-stu-id="5c500-364">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="5c500-365">Допустимые значения параметра [Office. CoercionType](xref:office.Office.CoercionType) зависят от узла.</span><span class="sxs-lookup"><span data-stu-id="5c500-365">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="5c500-366">Host (Узел)</span><span class="sxs-lookup"><span data-stu-id="5c500-366">Host</span></span></th> <th><span data-ttu-id="5c500-367">Поддерживаемое значение coercionType</span><span class="sxs-lookup"><span data-stu-id="5c500-367">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="5c500-368">Excel, PowerPoint, Project и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-368">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="5c500-369">`Office.CoercionType.Text`substring</span><span class="sxs-lookup"><span data-stu-id="5c500-369">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-370">Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-370">Excel and Word</span></span></td> <td><span data-ttu-id="5c500-371">`Office.CoercionType.Matrix`(массив массивов)</span><span class="sxs-lookup"><span data-stu-id="5c500-371">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-372">Access, Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-372">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="5c500-373">`Office.CoercionType.Table`(Объект TableData)</span><span class="sxs-lookup"><span data-stu-id="5c500-373">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-374">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-374">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="5c500-375">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-375">Word</span></span></td> <td><span data-ttu-id="5c500-376">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="5c500-376">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-377">PowerPoint и PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="5c500-377">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="5c500-378">Excel, PowerPoint и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-378">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'getSelectedDataAsync(coercionType, callback)'
    fullName: 'getSelectedDataAsync(coercionType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="5c500-379">Тип возвращаемой структуры данных.</span><span class="sxs-lookup"><span data-stu-id="5c500-379">The type of data structure to return.</span></span> <span data-ttu-id="5c500-380">В разделе "Примечания" представлены поддерживаемые типы приведения для каждого узла.</span><span class="sxs-lookup"><span data-stu-id="5c500-380">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - office.Office.CoercionType
        - id: callback
          description: '<span data-ttu-id="5c500-381">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-381">Optional.</span></span> <span data-ttu-id="5c500-382">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-382">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-383">.</span><span class="sxs-lookup"><span data-stu-id="5c500-383"></span></span> <span data-ttu-id="5c500-384">`value` Свойство Result представляет данные в текущем выделенном фрагменте.</span><span class="sxs-lookup"><span data-stu-id="5c500-384">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="5c500-385">Он возвращается в структуре данных или формате, указанном с помощью параметра coercionType.</span><span class="sxs-lookup"><span data-stu-id="5c500-385">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="5c500-386">(Дополнительные сведения о приведении типов данных см. в разделе Замечания.)</span><span class="sxs-lookup"><span data-stu-id="5c500-386">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync
    summary: <span data-ttu-id="5c500-387">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-387">Project documents only.</span></span> <span data-ttu-id="5c500-388">Получение идентификатора текущего выбранного ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-388">Get the current selected Resource's Id.</span></span>
    name: 'getSelectedResourceAsync(options, callback)'
    fullName: 'getSelectedResourceAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-389">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-389">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-390">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-390">Optional.</span></span> <span data-ttu-id="5c500-391">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-391">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-392">.</span><span class="sxs-lookup"><span data-stu-id="5c500-392"></span></span> <span data-ttu-id="5c500-393">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-393">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync_1
    summary: <span data-ttu-id="5c500-394">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-394">Project documents only.</span></span> <span data-ttu-id="5c500-395">Получение идентификатора текущего выбранного ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-395">Get the current selected Resource's Id.</span></span>
    name: getSelectedResourceAsync(callback)
    fullName: getSelectedResourceAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-396">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-396">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's 
          // currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page controls are
          // defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-397">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-397">Optional.</span></span> <span data-ttu-id="5c500-398">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-398">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-399">.</span><span class="sxs-lookup"><span data-stu-id="5c500-399"></span></span> <span data-ttu-id="5c500-400">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-400">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync
    summary: <span data-ttu-id="5c500-401">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-401">Project documents only.</span></span> <span data-ttu-id="5c500-402">Получение идентификатора текущей выбранной задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-402">Get the current selected Task's Id.</span></span>
    name: 'getSelectedTaskAsync(options, callback)'
    fullName: 'getSelectedTaskAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-403">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-403">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-404">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-404">Optional.</span></span> <span data-ttu-id="5c500-405">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-405">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-406">.</span><span class="sxs-lookup"><span data-stu-id="5c500-406"></span></span> <span data-ttu-id="5c500-407">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-407">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync_1
    summary: <span data-ttu-id="5c500-408">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-408">Project documents only.</span></span> <span data-ttu-id="5c500-409">Получение идентификатора текущей выбранной задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-409">Get the current selected Task's Id.</span></span>
    name: getSelectedTaskAsync(callback)
    fullName: getSelectedTaskAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-410">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-410">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets task properties by calling getTaskAsync.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-411">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-411">Optional.</span></span> <span data-ttu-id="5c500-412">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-412">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-413">.</span><span class="sxs-lookup"><span data-stu-id="5c500-413"></span></span> <span data-ttu-id="5c500-414">Значением `value` свойства Result является идентификатор GUID ресурса в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-414">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedViewAsync
    summary: '<span data-ttu-id="5c500-415">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-415">Project documents only.</span></span> <span data-ttu-id="5c500-416">Получение текущего выбранного типа представления (например,</span><span class="sxs-lookup"><span data-stu-id="5c500-416">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="5c500-417">Диаграмма Ганта) и имя представления.</span><span class="sxs-lookup"><span data-stu-id="5c500-417">Gantt) and View Name.</span></span>'
    name: 'getSelectedViewAsync(options, callback)'
    fullName: 'getSelectedViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-418">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-418">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-419">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-419">Optional.</span></span> <span data-ttu-id="5c500-420">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-420">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-421">.</span><span class="sxs-lookup"><span data-stu-id="5c500-421"></span></span> <span data-ttu-id="5c500-422">`value` Свойство Result содержит следующие свойства: `viewName` — имя представления в виде константы ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="5c500-422">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="5c500-423">`viewType`— Тип представления в виде целого значения константы ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="5c500-423">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getSelectedViewAsync_1
    summary: '<span data-ttu-id="5c500-424">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-424">Project documents only.</span></span> <span data-ttu-id="5c500-425">Получение текущего выбранного типа представления (например,</span><span class="sxs-lookup"><span data-stu-id="5c500-425">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="5c500-426">Диаграмма Ганта) и имя представления.</span><span class="sxs-lookup"><span data-stu-id="5c500-426">Gantt) and View Name.</span></span>'
    name: getSelectedViewAsync(callback)
    fullName: getSelectedViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-427">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-427">Examples</span></span>

          ```javascript
          // The following code example calls adds a ViewSelectionChanged event handler that
          // calls getSelectedViewAsync to get the name and type of the active view in the document.
          // The example assumes your add-in has a reference to the jQuery library and that
          // the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      Office.context.document.addHandlerAsync(
                          Office.EventType.ViewSelectionChanged,
                          getActiveView);
                      getActiveView();
                  });
              };

              // Get the active view's name and type.
              function getActiveView() {
                  Office.context.document.getSelectedViewAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var output = String.format(
                                  'View name: {0}<br/>View type: {1}',
                                  result.value.viewName, viewType);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-428">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-428">Optional.</span></span> <span data-ttu-id="5c500-429">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-429">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-430">.</span><span class="sxs-lookup"><span data-stu-id="5c500-430"></span></span> <span data-ttu-id="5c500-431">`value` Свойство Result содержит следующие свойства: `viewName` — имя представления в виде константы ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="5c500-431">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="5c500-432">`viewType`— Тип представления в виде целого значения константы ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="5c500-432">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync
    summary: '<span data-ttu-id="5c500-433">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-433">Project documents only.</span></span> <span data-ttu-id="5c500-434">Получение имени задачи, идентификатора задачи WSS и ResourceNames для данного taskId.</span><span class="sxs-lookup"><span data-stu-id="5c500-434">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, options, callback)'
    fullName: 'getTaskAsync(taskId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="5c500-435">Строка или значение идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-435">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: options
          description: '<span data-ttu-id="5c500-436">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-436">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-437">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-437">Optional.</span></span> <span data-ttu-id="5c500-438">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-438">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-439">.</span><span class="sxs-lookup"><span data-stu-id="5c500-439"></span></span> <span data-ttu-id="5c500-440">`value` Свойство Result содержит следующие свойства: `taskName` — имя задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-440">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="5c500-441">`wssTaskId`— Идентификатор задачи в синхронизированном списке задач SharePoint.</span><span class="sxs-lookup"><span data-stu-id="5c500-441">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="5c500-442">Если проект не синхронизирован со списком задач, значение будет 0.</span><span class="sxs-lookup"><span data-stu-id="5c500-442">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="5c500-443">`resourceNames`— Разделенный запятыми список имен ресурсов, назначенных задаче.</span><span class="sxs-lookup"><span data-stu-id="5c500-443">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync_1
    summary: '<span data-ttu-id="5c500-444">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-444">Project documents only.</span></span> <span data-ttu-id="5c500-445">Получение имени задачи, идентификатора задачи WSS и ResourceNames для данного taskId.</span><span class="sxs-lookup"><span data-stu-id="5c500-445">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, callback)'
    fullName: 'getTaskAsync(taskId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-446">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-446">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the task GUID of the currently
          // selected task. Then it calls getTaskAsync to get the properties for the task that are
          // available from the JavaScript API for Office.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="5c500-447">Строка или значение идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-447">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: callback
          description: '<span data-ttu-id="5c500-448">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-448">Optional.</span></span> <span data-ttu-id="5c500-449">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-449">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-450">.</span><span class="sxs-lookup"><span data-stu-id="5c500-450"></span></span> <span data-ttu-id="5c500-451">`value` Свойство Result содержит следующие свойства: `taskName` — имя задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-451">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="5c500-452">`wssTaskId`— Идентификатор задачи в синхронизированном списке задач SharePoint.</span><span class="sxs-lookup"><span data-stu-id="5c500-452">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="5c500-453">Если проект не синхронизирован со списком задач, значение будет 0.</span><span class="sxs-lookup"><span data-stu-id="5c500-453">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="5c500-454">`resourceNames`— Разделенный запятыми список имен ресурсов, назначенных задаче.</span><span class="sxs-lookup"><span data-stu-id="5c500-454">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync
    summary: |-
      <span data-ttu-id="5c500-455">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-455">Project documents only.</span></span> <span data-ttu-id="5c500-456">Получение идентификатора GUID задачи с указанным индексом в коллекции задач.</span><span class="sxs-lookup"><span data-stu-id="5c500-456">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="5c500-457">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-457">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, options, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="5c500-458">Индекс задачи в коллекции задач для проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-458">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="5c500-459">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-459">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-460">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-460">Optional.</span></span> <span data-ttu-id="5c500-461">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-461">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-462">.</span><span class="sxs-lookup"><span data-stu-id="5c500-462"></span></span> <span data-ttu-id="5c500-463">Значением `value` свойства является идентификатор GUID задачи в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-463">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync_1
    summary: |-
      <span data-ttu-id="5c500-464">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-464">Project documents only.</span></span> <span data-ttu-id="5c500-465">Получение идентификатора GUID задачи с указанным индексом в коллекции задач.</span><span class="sxs-lookup"><span data-stu-id="5c500-465">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="5c500-466">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-466">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-467">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-467">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the
          // maximum index in the project's task collection, and then
          // calls getTaskByIndexAsync to get the GUID for each task.
          // The example assumes that your add-in has a reference to the
          // jQuery library and that the following page controls are defined
          // in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="5c500-468">Индекс задачи в коллекции задач для проекта.</span><span class="sxs-lookup"><span data-stu-id="5c500-468">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="5c500-469">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-469">Optional.</span></span> <span data-ttu-id="5c500-470">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-470">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-471">.</span><span class="sxs-lookup"><span data-stu-id="5c500-471"></span></span> <span data-ttu-id="5c500-472">Значением `value` свойства является идентификатор GUID задачи в виде String.</span><span class="sxs-lookup"><span data-stu-id="5c500-472">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskFieldAsync
    summary: '<span data-ttu-id="5c500-473">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-473">Project documents only.</span></span> <span data-ttu-id="5c500-474">Получение поля задачи для указанного идентификатора задачи (например,</span><span class="sxs-lookup"><span data-stu-id="5c500-474">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="5c500-475">StartDate).</span><span class="sxs-lookup"><span data-stu-id="5c500-475">StartDate).</span></span>'
    name: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="5c500-476">Строка или значение идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-476">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-477">Поля задач.</span><span class="sxs-lookup"><span data-stu-id="5c500-477">Task Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="5c500-478">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-478">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-479">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-479">Optional.</span></span> <span data-ttu-id="5c500-480">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-480">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-481">.</span><span class="sxs-lookup"><span data-stu-id="5c500-481"></span></span> <span data-ttu-id="5c500-482">`value` Свойство Result содержит `fieldValue` свойство, которое представляет значение указанного поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-482">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskFieldAsync_1
    summary: '<span data-ttu-id="5c500-483">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-483">Project documents only.</span></span> <span data-ttu-id="5c500-484">Получение поля задачи для указанного идентификатора задачи (например,</span><span class="sxs-lookup"><span data-stu-id="5c500-484">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="5c500-485">StartDate).</span><span class="sxs-lookup"><span data-stu-id="5c500-485">StartDate).</span></span>'
    name: 'getTaskFieldAsync(taskId, fieldId, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-486">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-486">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets two task field values by calling getTaskFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected task.
              function getTaskFields(taskGuid) {
                  var output = '';
                  var targetFields = [Office.ProjectTaskFields.Priority, Office.ProjectTaskFields.PercentComplete];
                  var fieldValues = ['Priority: ', '% Complete: '];
                  var index = 0;
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // Get the field value. If the call is successful, then get the next field.
                      else {
                          Office.context.document.getTaskFieldAsync(
                              taskGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="5c500-487">Строка или значение идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-487">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-488">Поля задач.</span><span class="sxs-lookup"><span data-stu-id="5c500-488">Task Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="5c500-489">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-489">Optional.</span></span> <span data-ttu-id="5c500-490">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-490">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-491">.</span><span class="sxs-lookup"><span data-stu-id="5c500-491"></span></span> <span data-ttu-id="5c500-492">`value` Свойство Result содержит `fieldValue` свойство, которое представляет значение указанного поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-492">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync
    summary: '<span data-ttu-id="5c500-493">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-493">Project documents only.</span></span> <span data-ttu-id="5c500-494">Получение URL-адреса и имени списка для списка задач. MPP также синхронизируется.</span><span class="sxs-lookup"><span data-stu-id="5c500-494">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: 'getWSSUrlAsync(options, callback)'
    fullName: 'getWSSUrlAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="5c500-495">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-495">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-496">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-496">Optional.</span></span> <span data-ttu-id="5c500-497">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-497">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-498">.</span><span class="sxs-lookup"><span data-stu-id="5c500-498"></span></span> <span data-ttu-id="5c500-499">`value` Свойство Result содержит следующие свойства: `listName` — имя синхронизированного списка задач SharePoint.</span><span class="sxs-lookup"><span data-stu-id="5c500-499">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="5c500-500">`serverUrl`— URL-адрес синхронизированного списка задач SharePoint.</span><span class="sxs-lookup"><span data-stu-id="5c500-500">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync_1
    summary: '<span data-ttu-id="5c500-501">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-501">Project documents only.</span></span> <span data-ttu-id="5c500-502">Получение URL-адреса и имени списка для списка задач. MPP также синхронизируется.</span><span class="sxs-lookup"><span data-stu-id="5c500-502">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: getWSSUrlAsync(callback)
    fullName: getWSSUrlAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="5c500-503">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-503">Optional.</span></span> <span data-ttu-id="5c500-504">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-504">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-505">.</span><span class="sxs-lookup"><span data-stu-id="5c500-505"></span></span> <span data-ttu-id="5c500-506">`value` Свойство Result содержит следующие свойства: `listName` — имя синхронизированного списка задач SharePoint.</span><span class="sxs-lookup"><span data-stu-id="5c500-506">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="5c500-507">`serverUrl`— URL-адрес синхронизированного списка задач SharePoint.</span><span class="sxs-lookup"><span data-stu-id="5c500-507">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync
    summary: <span data-ttu-id="5c500-508">Переходит к указанному объекту или месту в документе.</span><span class="sxs-lookup"><span data-stu-id="5c500-508">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-509">**Набор**обязательных элементов: [не в наборе](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="5c500-509">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="5c500-510">PowerPoint не поддерживает метод goToByIdAsync в представлениях Master.</span><span class="sxs-lookup"><span data-stu-id="5c500-510">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="5c500-511">Поведение, вызванное параметром selectionMode, зависит от узла:</span><span class="sxs-lookup"><span data-stu-id="5c500-511">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="5c500-512">В Excel: `Office.SelectionMode.Selected` выделяет все содержимое в привязке или именованный элемент.</span><span class="sxs-lookup"><span data-stu-id="5c500-512">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="5c500-513">Office.SelectionMode.None для привязок к тексту выбирает ячейку; для привязок к матрицам, таблицам и именованных элементов — выбирает первую ячейку с данными (а не первую ячейку в столбце заготовка таблицы).</span><span class="sxs-lookup"><span data-stu-id="5c500-513">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="5c500-514">В PowerPoint: `Office.SelectionMode.Selected` выделяет заголовок слайда или первый элемент TextBox на слайде.</span><span class="sxs-lookup"><span data-stu-id="5c500-514">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="5c500-515">`Office.SelectionMode.None`ничего не выбирается.</span><span class="sxs-lookup"><span data-stu-id="5c500-515">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="5c500-516">В Word: `Office.SelectionMode.Selected` выделяет все содержимое в привязке.</span><span class="sxs-lookup"><span data-stu-id="5c500-516">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="5c500-517">Office.SelectionMode.None для текстовых привязок перемещает указатель в начало текста, а для привязок к матрицам и таблицам выбирает первую ячейку с данными (а не первую ячейку строки заголовка таблицы).</span><span class="sxs-lookup"><span data-stu-id="5c500-517">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>
    name: 'goToByIdAsync(id, goToType, options, callback)'
    fullName: 'goToByIdAsync(id, goToType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, options?: GoToByIdOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="5c500-518">Идентификатор объекта или расположения для перехода.</span><span class="sxs-lookup"><span data-stu-id="5c500-518">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: '<span data-ttu-id="5c500-519">Тип расположения, к которому выполняется переход.</span><span class="sxs-lookup"><span data-stu-id="5c500-519">The type of the location to go to.</span></span>'
          type:
            - office.Office.GoToType
        - id: options
          description: '<span data-ttu-id="5c500-520">Предоставляет параметры для выбора расположения, к которому выполняется переход.</span><span class="sxs-lookup"><span data-stu-id="5c500-520">Provides options for whether to select the location that is navigated to.</span></span>'
          type:
            - office.Office.GoToByIdOptions
        - id: callback
          description: '<span data-ttu-id="5c500-521">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-521">Optional.</span></span> <span data-ttu-id="5c500-522">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-522">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-523">.</span><span class="sxs-lookup"><span data-stu-id="5c500-523"></span></span> <span data-ttu-id="5c500-524">`value` Свойство Result является текущим представлением.</span><span class="sxs-lookup"><span data-stu-id="5c500-524">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync_1
    summary: <span data-ttu-id="5c500-525">Переходит к указанному объекту или месту в документе.</span><span class="sxs-lookup"><span data-stu-id="5c500-525">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-526">**Набор**обязательных элементов: [не в наборе](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="5c500-526">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="5c500-527">PowerPoint не поддерживает метод goToByIdAsync в представлениях Master.</span><span class="sxs-lookup"><span data-stu-id="5c500-527">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="5c500-528">Поведение, вызванное параметром selectionMode, зависит от узла:</span><span class="sxs-lookup"><span data-stu-id="5c500-528">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="5c500-529">В Excel: `Office.SelectionMode.Selected` выделяет все содержимое в привязке или именованный элемент.</span><span class="sxs-lookup"><span data-stu-id="5c500-529">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="5c500-530">Office.SelectionMode.None для привязок к тексту выбирает ячейку; для привязок к матрицам, таблицам и именованных элементов — выбирает первую ячейку с данными (а не первую ячейку в столбце заготовка таблицы).</span><span class="sxs-lookup"><span data-stu-id="5c500-530">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="5c500-531">В PowerPoint: `Office.SelectionMode.Selected` выделяет заголовок слайда или первый элемент TextBox на слайде.</span><span class="sxs-lookup"><span data-stu-id="5c500-531">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="5c500-532">`Office.SelectionMode.None`ничего не выбирается.</span><span class="sxs-lookup"><span data-stu-id="5c500-532">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="5c500-533">В Word: `Office.SelectionMode.Selected` выделяет все содержимое в привязке.</span><span class="sxs-lookup"><span data-stu-id="5c500-533">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="5c500-534">Office.SelectionMode.None для текстовых привязок перемещает указатель в начало текста, а для привязок к матрицам и таблицам выбирает первую ячейку с данными (а не первую ячейку строки заголовка таблицы).</span><span class="sxs-lookup"><span data-stu-id="5c500-534">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>

      #### <a name="examples"></a><span data-ttu-id="5c500-535">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-535">Examples</span></span>

      ```javascript
      // Go to a binding by id (Word and Excel)
      // The following example shows how to:
      // 1. Create a table binding using the addFromSelectionAsync method as a sample binding to work with.
      // 2. Specify that binding as the binding to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value on the add-in's page.
      function gotoBinding() {
          // Create a new table binding for the selected table.
          Office.context.document.bindings.addFromSelectionAsync("table",{ id: "MyTableBinding" }, function (asyncResult) {
          if (asyncResult.status == "failed") {
                    showMessage("Action failed with error: " + asyncResult.error.message);
                }
                else {
                    showMessage("Added new binding with type: " + asyncResult.value.type +" and id: " + asyncResult.value.id);
                }
          });

          // Go to binding by id.
          Office.context.document.goToByIdAsync("MyTableBinding", Office.GoToType.Binding, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to a table in a spreadsheet (Excel)
      // The following example shows how to:
      // 1. Specify a table by name as the table to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToTable() {
          Office.context.document.goToByIdAsync("Table1", Office.GoToType.NamedItem, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to the currently selected slide by id (PowerPoint)
      // The following example shows how to:
      // 1. Get the id of the currently selected slides using the getSelectedDataAsync method.
      // 2. Specify the returned id as the slide to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value of the stringified JSON object returned by asyncResult.value,
      //    which contains information about the selected slides, on the add-in's page.
      var firstSlideId = 0;
      function gotoSelectedSlide() {
          //Get currently selected slide's id
          Office.context.document.getSelectedDataAsync(Office.CoercionType.SlideRange, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  firstSlideId = asyncResult.value.slides[0].id;
                  app.showNotification(JSON.stringify(asyncResult.value));
              }
          });
          //Go to slide by id.
          Office.context.document.goToByIdAsync(firstSlideId, Office.GoToType.Slide, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  app.showNotification("Navigation successful");
              }
          });
      }

      // Go to slide by index (PowerPoint)
      // The following example shows how to:
      // 1. Specify the index of the first, last, previous, or next slide to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToSlideByIndex() {
          var goToFirst = Office.Index.First;
          var goToLast = Office.Index.Last;
          var goToPrevious = Office.Index.Previous;
          var goToNext = Office.Index.Next;

          Office.context.document.goToByIdAsync(goToNext, Office.GoToType.Index, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }
      ```
    name: 'goToByIdAsync(id, goToType, callback)'
    fullName: 'goToByIdAsync(id, goToType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="5c500-536">Идентификатор объекта или расположения для перехода.</span><span class="sxs-lookup"><span data-stu-id="5c500-536">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: '<span data-ttu-id="5c500-537">Тип расположения, к которому выполняется переход.</span><span class="sxs-lookup"><span data-stu-id="5c500-537">The type of the location to go to.</span></span>'
          type:
            - office.Office.GoToType
        - id: callback
          description: '<span data-ttu-id="5c500-538">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-538">Optional.</span></span> <span data-ttu-id="5c500-539">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-539">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-540">.</span><span class="sxs-lookup"><span data-stu-id="5c500-540"></span></span> <span data-ttu-id="5c500-541">`value` Свойство Result является текущим представлением.</span><span class="sxs-lookup"><span data-stu-id="5c500-541">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.removeHandlerAsync
    summary: <span data-ttu-id="5c500-542">Удаляет обработчик событий для указанного типа события.</span><span class="sxs-lookup"><span data-stu-id="5c500-542">Removes an event handler for the specified event type.</span></span>
    remarks: '<span data-ttu-id="5c500-543">**Набор требований**: [документевентс](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="5c500-543">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>'
    name: 'removeHandlerAsync(eventType, options, callback)'
    fullName: 'removeHandlerAsync(eventType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, options?: RemoveHandlerOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="5c500-544">Тип события.</span><span class="sxs-lookup"><span data-stu-id="5c500-544">The event type.</span></span> <span data-ttu-id="5c500-545">Для Document можно указать "Document. SelectionChanged" или "Document. ActiveViewChanged".</span><span class="sxs-lookup"><span data-stu-id="5c500-545">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: options
          description: '<span data-ttu-id="5c500-546">Предоставляет параметры для определения обработчика событий или обработчиков, которые удаляются.</span><span class="sxs-lookup"><span data-stu-id="5c500-546">Provides options to determine which event handler or handlers are removed.</span></span>'
          type:
            - office.Office.RemoveHandlerOptions
        - id: callback
          description: '<span data-ttu-id="5c500-547">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-547">Optional.</span></span> <span data-ttu-id="5c500-548">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-548">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-549">.</span><span class="sxs-lookup"><span data-stu-id="5c500-549"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.removeHandlerAsync_1
    summary: <span data-ttu-id="5c500-550">Удаляет обработчик событий для указанного типа события.</span><span class="sxs-lookup"><span data-stu-id="5c500-550">Removes an event handler for the specified event type.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-551">**Набор требований**: [документевентс](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="5c500-551">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      #### <a name="examples"></a><span data-ttu-id="5c500-552">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-552">Examples</span></span>

      ```javascript
      // The following example removes the event handler named 'MyHandler'.
      function removeSelectionChangedEventHandler() {
          Office.context.document.removeHandlerAsync(Office.EventType.DocumentSelectionChanged, {handler:MyHandler});
      }

      function MyHandler(eventArgs) {
          doSomethingWithDocument(eventArgs.document);
      }
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the
      // ResourceSelectionChanged event and removeHandlerAsync to remove the handler.
      // When a resource is selected in a resource view, the handler displays the
      // resource GUID. When the handler is removed, the GUID is not displayed.
      // The example assumes that your add-in has a reference to the jQuery library and
      // that the following page control is defined in the content div in the page body:
      // <input id="remove-handler" type="button" value="Remove handler" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
                  $('#remove-handler').click(removeEventHandler);
              });
          };

          // Remove the event handler.
          function removeEventHandler() {
              Office.context.document.removeHandlerAsync(
                  Office.EventType.ResourceSelectionChanged,
                  {handler:getResourceGuid,
                  asyncContext:'The handler is removed.'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#remove-handler').attr('disabled', 'disabled');
                          $('#message').html(result.asyncContext);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'removeHandlerAsync(eventType, callback)'
    fullName: 'removeHandlerAsync(eventType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="5c500-553">Тип события.</span><span class="sxs-lookup"><span data-stu-id="5c500-553">The event type.</span></span> <span data-ttu-id="5c500-554">Для Document можно указать "Document. SelectionChanged" или "Document. ActiveViewChanged".</span><span class="sxs-lookup"><span data-stu-id="5c500-554">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: callback
          description: '<span data-ttu-id="5c500-555">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-555">Optional.</span></span> <span data-ttu-id="5c500-556">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-556">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-557">.</span><span class="sxs-lookup"><span data-stu-id="5c500-557"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync
    summary: |-
      <span data-ttu-id="5c500-558">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-558">Project documents only.</span></span> <span data-ttu-id="5c500-559">Задание поля ресурса для указанного идентификатора ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-559">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="5c500-560">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-560">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="5c500-561">Строка или значение идентификатора ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-561">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-562">Поля ресурсов.</span><span class="sxs-lookup"><span data-stu-id="5c500-562">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="5c500-563">Значение целевого поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-563">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="5c500-564">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-564">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-565">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-565">Optional.</span></span> <span data-ttu-id="5c500-566">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-566">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-567">.</span><span class="sxs-lookup"><span data-stu-id="5c500-567"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync_1
    summary: |-
      <span data-ttu-id="5c500-568">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-568">Project documents only.</span></span> <span data-ttu-id="5c500-569">Задание поля ресурса для указанного идентификатора ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-569">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="5c500-570">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-570">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-571">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-571">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's
          // currently selected in a resource view. Then it sets two resource field values by calling
          // setResourceFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the addHandlerAsync
          // method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setResourceInfo);
                  });
              };

              // Get the GUID of the resource, and then get the resource fields.
              function setResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          setResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected resource.
              function setResourceFields(resourceGuid) {
                  var targetFields = [Office.ProjectResourceFields.StandardRate, Office.ProjectResourceFields.Notes];
                  var fieldValues = [.28, 'Notes for the resource.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setResourceFieldAsync(
                          resourceGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="5c500-572">Строка или значение идентификатора ресурса.</span><span class="sxs-lookup"><span data-stu-id="5c500-572">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-573">Поля ресурсов.</span><span class="sxs-lookup"><span data-stu-id="5c500-573">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="5c500-574">Значение целевого поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-574">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="5c500-575">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-575">Optional.</span></span> <span data-ttu-id="5c500-576">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-576">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-577">.</span><span class="sxs-lookup"><span data-stu-id="5c500-577"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync
    summary: <span data-ttu-id="5c500-578">ЗаПисывает указанные данные в текущий выделенный фрагмент.</span><span class="sxs-lookup"><span data-stu-id="5c500-578">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-579">**Наборы требований**: [хтмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span><span class="sxs-lookup"><span data-stu-id="5c500-579">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span></span><!-- --><span data-ttu-id="5c500-580">(при использовании`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="5c500-580">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="5c500-581">), [Использовать imagecoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (при использовании`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="5c500-581">), [ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="5c500-582">), [МатрикскоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (при использовании`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="5c500-582">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="5c500-583">), [УксмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (при использовании`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="5c500-583">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="5c500-584">), [Выделение](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="5c500-584">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="5c500-585">, [ТаблекоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (при использовании`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="5c500-585">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="5c500-586">), [ТексткоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (при использовании`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="5c500-586">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="5c500-587">)</span><span class="sxs-lookup"><span data-stu-id="5c500-587"></span></span>

      <span data-ttu-id="5c500-588">**Поведение, связанное с определенными приложениями**</span><span class="sxs-lookup"><span data-stu-id="5c500-588">**Application-specific behaviors**</span></span>

      <span data-ttu-id="5c500-589">При записи данных в выделенный фрагмент применяются следующие действия, зависящие от приложения.</span><span class="sxs-lookup"><span data-stu-id="5c500-589">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="5c500-590">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-590">Word</span></span></td> <td><span data-ttu-id="5c500-591">Если выделенный фрагмент отсутствует, а точка вставки находится в допустимом расположении, то `data` заданное положение вставляется в точку вставки.</span><span class="sxs-lookup"><span data-stu-id="5c500-591">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="5c500-592">Если `data` строка является строкой, вставляется указанный текст.</span><span class="sxs-lookup"><span data-stu-id="5c500-592">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-593">Если `data` это массив массивов ("Matrix") или объект TableData, вставляется новая таблица Word.</span><span class="sxs-lookup"><span data-stu-id="5c500-593">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-594">Если `data` это HTML-код, вставляется указанный HTML-код.</span><span class="sxs-lookup"><span data-stu-id="5c500-594">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="5c500-595">(**Важно**! если какой-либо из вставленных вами HTML не является допустимым, Word не будет возникнет ошибка.</span><span class="sxs-lookup"><span data-stu-id="5c500-595">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="5c500-596">Word вставит как можно больше кода HTML, так как он может опускать недопустимые данные.</span><span class="sxs-lookup"><span data-stu-id="5c500-596">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-597">Если `data` это Office Open XML, вставляется указанный XML-код.</span><span class="sxs-lookup"><span data-stu-id="5c500-597">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-598">Если `data` это поток образа с кодировкой base64, вставляется указанный образ.</span><span class="sxs-lookup"><span data-stu-id="5c500-598">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-599">Если выделен фрагмент</span><span class="sxs-lookup"><span data-stu-id="5c500-599">If there is a selection</span></span></td> <td><span data-ttu-id="5c500-600">Он будет заменен на указанные `data` ниже правила, описанные ниже.</span><span class="sxs-lookup"><span data-stu-id="5c500-600">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-601">Вставка изображений</span><span class="sxs-lookup"><span data-stu-id="5c500-601">Insert images</span></span></td> <td><span data-ttu-id="5c500-602">Вставленные изображения помещаются в тексте.</span><span class="sxs-lookup"><span data-stu-id="5c500-602">Inserted images are placed inline.</span></span> <span data-ttu-id="5c500-603">Параметры imageLeft и imageTop игнорируются.</span><span class="sxs-lookup"><span data-stu-id="5c500-603">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="5c500-604">Пропорции изображения всегда блокируются.</span><span class="sxs-lookup"><span data-stu-id="5c500-604">The image aspect ratio is always locked.</span></span> <span data-ttu-id="5c500-605">Если задан только параметр imageWidth или imageHeight, второе значение будет подобрано автоматически с учетом исходных пропорций.</span><span class="sxs-lookup"><span data-stu-id="5c500-605">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="5c500-606">Excel</span><span class="sxs-lookup"><span data-stu-id="5c500-606">Excel</span></span></td> <td><span data-ttu-id="5c500-607">Если выбрана одна ячейка</span><span class="sxs-lookup"><span data-stu-id="5c500-607">If a single cell is selected</span></span></td> <td><span data-ttu-id="5c500-608">Если `data` строка является строкой, указанный текст вставляется в качестве значения текущей ячейки.</span><span class="sxs-lookup"><span data-stu-id="5c500-608">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-609">Если `data` это массив массивов ("Matrix"), вставляется заданный набор строк и столбцов, если при этом не будут перезаписаны другие данные в соседних ячейках.</span><span class="sxs-lookup"><span data-stu-id="5c500-609">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-610">Если `data` объект TableData, вставляется новая таблица Excel с заданным набором строк и заголовков, если при этом не будут перезаписаны другие данные в соседних ячейках.</span><span class="sxs-lookup"><span data-stu-id="5c500-610">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-611">Если выделено несколько ячеек</span><span class="sxs-lookup"><span data-stu-id="5c500-611">If multiple cells are selected</span></span></td> <td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="5c500-612">, возвращается ошибка.</span><span class="sxs-lookup"><span data-stu-id="5c500-612">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="5c500-613">значения выбранных ячеек обновляются в соответствии со значениями в`data`</span><span class="sxs-lookup"><span data-stu-id="5c500-613">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="5c500-614">.</span><span class="sxs-lookup"><span data-stu-id="5c500-614"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-615">Вставка изображений</span><span class="sxs-lookup"><span data-stu-id="5c500-615">Insert images</span></span></td> <td><span data-ttu-id="5c500-616">Вставляются плавающие изображения.</span><span class="sxs-lookup"><span data-stu-id="5c500-616">Inserted images are floating.</span></span> <span data-ttu-id="5c500-617">Параметры положения imageLeft и imageTop указываются относительно выделенных ячеек.</span><span class="sxs-lookup"><span data-stu-id="5c500-617">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="5c500-618">Отрицательные значения imageLeft и imageTop допустимы и могут быть откорректированы Excel для размещения изображения в пределах листа.</span><span class="sxs-lookup"><span data-stu-id="5c500-618">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="5c500-619">Пропорции изображения блокируются, если не указан каждый из параметров imageWidth и imageHeight.</span><span class="sxs-lookup"><span data-stu-id="5c500-619">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="5c500-620">Если задан только параметр imageWidth или только параметр imageHeight, второе значение будет подобрано автоматически с учетом исходных пропорций.</span><span class="sxs-lookup"><span data-stu-id="5c500-620">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-621">Все остальные случаи</span><span class="sxs-lookup"><span data-stu-id="5c500-621">All other cases</span></span></td> <td><span data-ttu-id="5c500-622">Возвращается ошибка.</span><span class="sxs-lookup"><span data-stu-id="5c500-622">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="5c500-623">Excel Online</span><span class="sxs-lookup"><span data-stu-id="5c500-623">Excel Online</span></span></td> <td><span data-ttu-id="5c500-624">В дополнение к поведению, описанному в вышеприведенном приложении Excel, эти пределы применяются при записи данных в Excel Online</span><span class="sxs-lookup"><span data-stu-id="5c500-624">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel Online</span></span></td> <td><span data-ttu-id="5c500-625">Общее число ячеек, которые можно записать на лист с `data` параметром, не может превышать 20 000 в отдельном вызове этого метода.</span><span class="sxs-lookup"><span data-stu-id="5c500-625">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-626">Количество групп форматирования, переданных в параметр `cellFormat` , не может превышать 100.</span><span class="sxs-lookup"><span data-stu-id="5c500-626">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="5c500-627">Одна группа форматирования состоит из набора вариантов форматирования, применяемого к указанному диапазону ячеек.</span><span class="sxs-lookup"><span data-stu-id="5c500-627">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="5c500-628">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="5c500-628">PowerPoint</span></span></td> <td><span data-ttu-id="5c500-629">Вставка изображения</span><span class="sxs-lookup"><span data-stu-id="5c500-629">Insert image</span></span></td> <td><span data-ttu-id="5c500-630">Вставляются плавающие изображения.</span><span class="sxs-lookup"><span data-stu-id="5c500-630">Inserted images are floating.</span></span> <span data-ttu-id="5c500-631">Параметры Position imageLeft и imageTop необязательны, но если указаны, оба должны присутствовать.</span><span class="sxs-lookup"><span data-stu-id="5c500-631">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="5c500-632">Если указано одно значение, оно игнорируется.</span><span class="sxs-lookup"><span data-stu-id="5c500-632">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="5c500-633">Отрицательные значения imageLeft и imageTop допустимы и позволяют разместить изображение за пределами слайда.</span><span class="sxs-lookup"><span data-stu-id="5c500-633">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="5c500-634">Если не указано ни одного необязательного параметра, а слайд содержит заполнитель, изображение заменит заполнитель в слайде.</span><span class="sxs-lookup"><span data-stu-id="5c500-634">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="5c500-635">Пропорции изображения блокируются, если не указан каждый из параметров imageWidth и imageHeight.</span><span class="sxs-lookup"><span data-stu-id="5c500-635">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="5c500-636">Если задан только параметр imageWidth или только параметр imageHeight, второе значение будет подобрано автоматически с учетом исходных пропорций.</span><span class="sxs-lookup"><span data-stu-id="5c500-636">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="5c500-637">Допустимые значения параметра [Office. CoercionType](xref:office.Office.CoercionType) зависят от узла.</span><span class="sxs-lookup"><span data-stu-id="5c500-637">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="5c500-638">Host (Узел)</span><span class="sxs-lookup"><span data-stu-id="5c500-638">Host</span></span></th> <th><span data-ttu-id="5c500-639">Поддерживаемое значение coercionType</span><span class="sxs-lookup"><span data-stu-id="5c500-639">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="5c500-640">Excel, PowerPoint, Project и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-640">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="5c500-641">`Office.CoercionType.Text`substring</span><span class="sxs-lookup"><span data-stu-id="5c500-641">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-642">Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-642">Excel and Word</span></span></td> <td><span data-ttu-id="5c500-643">`Office.CoercionType.Matrix`(массив массивов)</span><span class="sxs-lookup"><span data-stu-id="5c500-643">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-644">Access, Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-644">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="5c500-645">`Office.CoercionType.Table`(Объект TableData)</span><span class="sxs-lookup"><span data-stu-id="5c500-645">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-646">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-646">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="5c500-647">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-647">Word</span></span></td> <td><span data-ttu-id="5c500-648">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="5c500-648">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-649">PowerPoint и PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="5c500-649">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="5c500-650">Excel, PowerPoint и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-650">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="5c500-651">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-651">Examples</span></span>

      ```javascript
      // The following example sets the selected text or cell to "Hello World!", 
      // and if that fails, displays the value of the error.message property.
      function writeText() {
          Office.context.document.setSelectedDataAsync("Hello World!",
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // Specifying the optional coercionType parameter lets you specify the kind of data you want to write
      // to a selection. The following example writes data as an array of three rows of two columns, 
      // specifying the coercionType as `Matrix` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeMatrix() {
          Office.context.document.setSelectedDataAsync(
              [["Red", "Rojo"], ["Green", "Verde"], ["Blue", "Azul"]],
              {coercionType: Office.CoercionType.Matrix}
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // The following example writes data as a one column table with a header and four rows, 
      // specifying the coercionType as `Table` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeTable() {
          // Build table.
          var myTable = new Office.TableData();
          myTable.headers = [["Cities"]];
          myTable.rows = [['Berlin'], ['Roma'], ['Tokyo'], ['Seattle']];

          // Write table.
          Office.context.document.setSelectedDataAsync(myTable, {coercionType: Office.CoercionType.Table},
              function (result) {
                  var error = result.error
                  if (result.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word if you want to write HTML to the selection, you can specify the coercionType parameter as `Html`
      // as shown in the following example, which uses HTML <b> tags to make "Hello" bold.
      function writeHtmlData() {
          Office.context.document.setSelectedDataAsync(
              "<b>Hello</b> World!", {coercionType: Office.CoercionType.Html}, function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write('Error: ' + asyncResult.error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word, PowerPoint, or Excel, if you want to write an image to the selection, you can specify the coercionType
      // parameter as `Image` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertPictureAtSelection(base64EncodedImageStr) {

          Office.context.document.setSelectedDataAsync(base64EncodedImageStr, {
              coercionType: Office.CoercionType.Image,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 100,
              imageHeight: 100
          },
          function (asyncResult) {
              if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                  console.log("Action failed with error: " + asyncResult.error.message);
              }
          });
      }

      // In Word, PowerPoint, or Excel, if you want to write an scalable vector graphic (SVG) to the selection, you can specify the 
      // coercionType parameter as `XmlSvg` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertSvgAtSelection(base64EncodedImageStr) {
          Office.context.document.setSelectedDataAsync(getImageAsBase64String(), {
              coercionType: Office.CoercionType.XmlSvg,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 400
          },
              function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      console.log(asyncResult.error.message);
                  }
              });
      }
      ```
    name: 'setSelectedDataAsync(data, options, callback)'
    fullName: 'setSelectedDataAsync(data, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], options?: SetSelectedDataOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="5c500-652">Данные, которые необходимо задать.</span><span class="sxs-lookup"><span data-stu-id="5c500-652">The data to be set.</span></span> <span data-ttu-id="5c500-653">Строка или значение [Office. CoercionType](xref:office.Office.CoercionType) , 2D-массив или объект TableData.</span><span class="sxs-lookup"><span data-stu-id="5c500-653">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="5c500-654">Если значение передано `data` :</span><span class="sxs-lookup"><span data-stu-id="5c500-654">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="5c500-655">Строка. Записывается обычный текст или другие данные, которые могут быть приведены к типу string.</span><span class="sxs-lookup"><span data-stu-id="5c500-655">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="5c500-656">В Excel можно также указать данные в виде допустимой формулы, чтобы добавить эту формулу в выбранную ячейку.</span><span class="sxs-lookup"><span data-stu-id="5c500-656">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="5c500-657">Например, если для data указать "=SUM(A1:A5)", будут получены итоговые значения в указанном диапазоне.</span><span class="sxs-lookup"><span data-stu-id="5c500-657">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="5c500-658">Если задать формулу в связанной ячейке, добавленную (или существующую) формулу в этой ячейке будет невозможно считать.</span><span class="sxs-lookup"><span data-stu-id="5c500-658">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="5c500-659">При вызове метода Document.getSelectedDataAsync в выделенной ячейке для считывания ее данных этот метод может возвращать только данные, отображаемые в ячейке (результат формулы).</span><span class="sxs-lookup"><span data-stu-id="5c500-659">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="5c500-660">Массив массивов ("matrix"). Будут вставлены табличные данные без заголовков.</span><span class="sxs-lookup"><span data-stu-id="5c500-660">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="5c500-661">Например, чтобы записать данные в три строки в двух столбцах, можно \[ \[передать массив следующим образом: "R1C1", "\]R1C2", \["R2C1", "R2C2"\], \["R3C1", "R3C2".\]\]</span><span class="sxs-lookup"><span data-stu-id="5c500-661">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="5c500-662">Чтобы записать один столбец из трех строк, передайте массив следующим образом: \[ \["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="5c500-662">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="5c500-663">В Excel вы также можете указать data как массив массивов, содержащих допустимые формулы, чтобы добавить их в выбранные ячейки.</span><span class="sxs-lookup"><span data-stu-id="5c500-663">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="5c500-664">Например, если никакие другие данные не будут перезаписаны, то для \[ \[параметра data — "= Sum (a1: A5)", "= СРЗНАЧ (a1:\] \] A5)" будут добавлены эти две формулы к выделенному фрагменту.</span><span class="sxs-lookup"><span data-stu-id="5c500-664">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="5c500-665">Как и при указании формулы в одной ячейке в текстовом виде, добавленные (или существующие) формулы невозможно считывать после того, как они заданы. Вы можете считывать только результаты формул.</span><span class="sxs-lookup"><span data-stu-id="5c500-665">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="5c500-666">Объект TableData Вставляются табличные данные с заголовками.</span><span class="sxs-lookup"><span data-stu-id="5c500-666">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="5c500-667">Если вы укажете в Excel формулы в объекте TableData, который вы передаете для параметра data, вы можете не получить ожидаемые результаты, потому что функция "вычисляемые столбцы" Excel автоматически копирует формулы в столбце.</span><span class="sxs-lookup"><span data-stu-id="5c500-667">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="5c500-668">Чтобы обойти эту проблему, если вы хотите `data` записать формулы в выбранную таблицу, попробуйте указать данные в виде массива массивов (вместо объекта TableData) и укажите CoercionType как Microsoft. Office. Matrix или "Matrix".</span><span class="sxs-lookup"><span data-stu-id="5c500-668">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="5c500-669">Однако этот метод блокирует функцию "вычисляемые столбцы" только при выполнении одного из следующих условий: (1) вы пишете во все ячейки столбца или (2) в столбце уже есть по крайней мере две разные формулы.</span><span class="sxs-lookup"><span data-stu-id="5c500-669">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: options
          description: <span data-ttu-id="5c500-670">Предоставляет варианты вставки данных в выделенный фрагмент.</span><span class="sxs-lookup"><span data-stu-id="5c500-670">Provides options for how to insert data to the selection.</span></span>
          type:
            - office.Office.SetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="5c500-671">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-671">Optional.</span></span> <span data-ttu-id="5c500-672">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-672">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-673">.</span><span class="sxs-lookup"><span data-stu-id="5c500-673"></span></span> <span data-ttu-id="5c500-674">Свойство AsyncResult. Value всегда возвращает значение undefine, так как нет объекта или данных для извлечения.</span><span class="sxs-lookup"><span data-stu-id="5c500-674">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync_1
    summary: <span data-ttu-id="5c500-675">ЗаПисывает указанные данные в текущий выделенный фрагмент.</span><span class="sxs-lookup"><span data-stu-id="5c500-675">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="5c500-676">**Наборы требований**: [хтмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span><span class="sxs-lookup"><span data-stu-id="5c500-676">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span></span><!-- --><span data-ttu-id="5c500-677">(при использовании`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="5c500-677">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="5c500-678">), [Использовать imagecoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (при использовании`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="5c500-678">), [ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="5c500-679">), [МатрикскоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (при использовании`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="5c500-679">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="5c500-680">), [УксмлкоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (при использовании`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="5c500-680">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="5c500-681">), [Выделение](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="5c500-681">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="5c500-682">, [ТаблекоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (при использовании`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="5c500-682">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="5c500-683">), [ТексткоерЦион](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (при использовании`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="5c500-683">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="5c500-684">)</span><span class="sxs-lookup"><span data-stu-id="5c500-684"></span></span>

      <span data-ttu-id="5c500-685">**Поведение, связанное с определенными приложениями**</span><span class="sxs-lookup"><span data-stu-id="5c500-685">**Application-specific behaviors**</span></span>

      <span data-ttu-id="5c500-686">При записи данных в выделенный фрагмент применяются следующие действия, зависящие от приложения.</span><span class="sxs-lookup"><span data-stu-id="5c500-686">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="5c500-687">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-687">Word</span></span></td> <td><span data-ttu-id="5c500-688">Если выделенный фрагмент отсутствует, а точка вставки находится в допустимом расположении, то `data` заданное положение вставляется в точку вставки.</span><span class="sxs-lookup"><span data-stu-id="5c500-688">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="5c500-689">Если `data` строка является строкой, вставляется указанный текст.</span><span class="sxs-lookup"><span data-stu-id="5c500-689">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-690">Если `data` это массив массивов ("Matrix") или объект TableData, вставляется новая таблица Word.</span><span class="sxs-lookup"><span data-stu-id="5c500-690">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-691">Если `data` это HTML-код, вставляется указанный HTML-код.</span><span class="sxs-lookup"><span data-stu-id="5c500-691">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="5c500-692">(**Важно**! если какой-либо из вставленных вами HTML не является допустимым, Word не будет возникнет ошибка.</span><span class="sxs-lookup"><span data-stu-id="5c500-692">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="5c500-693">Word вставит как можно больше кода HTML, так как он может опускать недопустимые данные.</span><span class="sxs-lookup"><span data-stu-id="5c500-693">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-694">Если `data` это Office Open XML, вставляется указанный XML-код.</span><span class="sxs-lookup"><span data-stu-id="5c500-694">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-695">Если `data` это поток образа с кодировкой base64, вставляется указанный образ.</span><span class="sxs-lookup"><span data-stu-id="5c500-695">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td></tr></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-696">Если выделен фрагмент</span><span class="sxs-lookup"><span data-stu-id="5c500-696">If there is a selection</span></span></td> <td><span data-ttu-id="5c500-697">Он будет заменен на указанные `data` ниже правила, описанные ниже.</span><span class="sxs-lookup"><span data-stu-id="5c500-697">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-698">Вставка изображений</span><span class="sxs-lookup"><span data-stu-id="5c500-698">Insert images</span></span></td> <td><span data-ttu-id="5c500-699">Вставленные изображения помещаются в тексте.</span><span class="sxs-lookup"><span data-stu-id="5c500-699">Inserted images are placed inline.</span></span> <span data-ttu-id="5c500-700">Параметры imageLeft и imageTop игнорируются.</span><span class="sxs-lookup"><span data-stu-id="5c500-700">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="5c500-701">Пропорции изображения всегда блокируются.</span><span class="sxs-lookup"><span data-stu-id="5c500-701">The image aspect ratio is always locked.</span></span> <span data-ttu-id="5c500-702">Если задан только параметр imageWidth или imageHeight, второе значение будет подобрано автоматически с учетом исходных пропорций.</span><span class="sxs-lookup"><span data-stu-id="5c500-702">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="5c500-703">Excel</span><span class="sxs-lookup"><span data-stu-id="5c500-703">Excel</span></span></td> <td><span data-ttu-id="5c500-704">Если выбрана одна ячейка</span><span class="sxs-lookup"><span data-stu-id="5c500-704">If a single cell is selected</span></span></td> <td><span data-ttu-id="5c500-705">Если `data` строка является строкой, указанный текст вставляется в качестве значения текущей ячейки.</span><span class="sxs-lookup"><span data-stu-id="5c500-705">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-706">Если `data` это массив массивов ("Matrix"), вставляется заданный набор строк и столбцов, если при этом не будут перезаписаны другие данные в соседних ячейках.</span><span class="sxs-lookup"><span data-stu-id="5c500-706">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-707">Если `data` объект TableData, вставляется новая таблица Excel с заданным набором строк и заголовков, если при этом не будут перезаписаны другие данные в соседних ячейках.</span><span class="sxs-lookup"><span data-stu-id="5c500-707">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-708">Если выделено несколько ячеек</span><span class="sxs-lookup"><span data-stu-id="5c500-708">If multiple cells are selected</span></span></td><td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="5c500-709">, возвращается ошибка.</span><span class="sxs-lookup"><span data-stu-id="5c500-709">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="5c500-710">значения выбранных ячеек обновляются в соответствии со значениями в`data`</span><span class="sxs-lookup"><span data-stu-id="5c500-710">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="5c500-711">.</span><span class="sxs-lookup"><span data-stu-id="5c500-711"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-712">Вставка изображений</span><span class="sxs-lookup"><span data-stu-id="5c500-712">Insert images</span></span></td> <td><span data-ttu-id="5c500-713">Вставляются плавающие изображения.</span><span class="sxs-lookup"><span data-stu-id="5c500-713">Inserted images are floating.</span></span> <span data-ttu-id="5c500-714">Параметры положения imageLeft и imageTop указываются относительно выделенных ячеек.</span><span class="sxs-lookup"><span data-stu-id="5c500-714">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="5c500-715">Отрицательные значения imageLeft и imageTop допустимы и могут быть откорректированы Excel для размещения изображения в пределах листа.</span><span class="sxs-lookup"><span data-stu-id="5c500-715">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="5c500-716">Пропорции изображения блокируются, если не указан каждый из параметров imageWidth и imageHeight.</span><span class="sxs-lookup"><span data-stu-id="5c500-716">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="5c500-717">Если задан только параметр imageWidth или только параметр imageHeight, второе значение будет подобрано автоматически с учетом исходных пропорций.</span><span class="sxs-lookup"><span data-stu-id="5c500-717">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="5c500-718">Все остальные случаи</span><span class="sxs-lookup"><span data-stu-id="5c500-718">All other cases</span></span></td> <td><span data-ttu-id="5c500-719">Возвращается ошибка.</span><span class="sxs-lookup"><span data-stu-id="5c500-719">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="5c500-720">Excel Online</span><span class="sxs-lookup"><span data-stu-id="5c500-720">Excel Online</span></span></td> <td><span data-ttu-id="5c500-721">В дополнение к поведению, описанному в вышеприведенном приложении Excel, эти пределы применяются при записи данных в Excel Online</span><span class="sxs-lookup"><span data-stu-id="5c500-721">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel Online</span></span></td> <td><span data-ttu-id="5c500-722">Общее число ячеек, которые можно записать на лист с `data` параметром, не может превышать 20 000 в отдельном вызове этого метода.</span><span class="sxs-lookup"><span data-stu-id="5c500-722">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="5c500-723">Количество групп форматирования, переданных в параметр `cellFormat` , не может превышать 100.</span><span class="sxs-lookup"><span data-stu-id="5c500-723">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="5c500-724">Одна группа форматирования состоит из набора вариантов форматирования, применяемого к указанному диапазону ячеек.</span><span class="sxs-lookup"><span data-stu-id="5c500-724">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="5c500-725">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="5c500-725">PowerPoint</span></span></td> <td><span data-ttu-id="5c500-726">Вставка изображения</span><span class="sxs-lookup"><span data-stu-id="5c500-726">Insert image</span></span></td> <td><span data-ttu-id="5c500-727">Вставляются плавающие изображения.</span><span class="sxs-lookup"><span data-stu-id="5c500-727">Inserted images are floating.</span></span> <span data-ttu-id="5c500-728">Параметры Position imageLeft и imageTop необязательны, но если указаны, оба должны присутствовать.</span><span class="sxs-lookup"><span data-stu-id="5c500-728">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="5c500-729">Если указано одно значение, оно игнорируется.</span><span class="sxs-lookup"><span data-stu-id="5c500-729">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="5c500-730">Отрицательные значения imageLeft и imageTop допустимы и позволяют разместить изображение за пределами слайда.</span><span class="sxs-lookup"><span data-stu-id="5c500-730">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="5c500-731">Если не указано ни одного необязательного параметра, а слайд содержит заполнитель, изображение заменит заполнитель в слайде.</span><span class="sxs-lookup"><span data-stu-id="5c500-731">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="5c500-732">Пропорции изображения блокируются, если не указан каждый из параметров imageWidth и imageHeight.</span><span class="sxs-lookup"><span data-stu-id="5c500-732">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="5c500-733">Если задан только параметр imageWidth или только параметр imageHeight, второе значение будет подобрано автоматически с учетом исходных пропорций.</span><span class="sxs-lookup"><span data-stu-id="5c500-733">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="5c500-734">Допустимые значения параметра [Office. CoercionType](xref:office.Office.CoercionType) зависят от узла.</span><span class="sxs-lookup"><span data-stu-id="5c500-734">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="5c500-735">Host (Узел)</span><span class="sxs-lookup"><span data-stu-id="5c500-735">Host</span></span></th> <th><span data-ttu-id="5c500-736">Поддерживаемое значение coercionType</span><span class="sxs-lookup"><span data-stu-id="5c500-736">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="5c500-737">Excel, PowerPoint, Project и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-737">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="5c500-738">`Office.CoercionType.Text`substring</span><span class="sxs-lookup"><span data-stu-id="5c500-738">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-739">Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-739">Excel and Word</span></span></td> <td><span data-ttu-id="5c500-740">`Office.CoercionType.Matrix`(массив массивов)</span><span class="sxs-lookup"><span data-stu-id="5c500-740">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-741">Access, Excel и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-741">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="5c500-742">`Office.CoercionType.Table`(Объект TableData)</span><span class="sxs-lookup"><span data-stu-id="5c500-742">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-743">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-743">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="5c500-744">Word</span><span class="sxs-lookup"><span data-stu-id="5c500-744">Word</span></span></td> <td><span data-ttu-id="5c500-745">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="5c500-745">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="5c500-746">PowerPoint и PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="5c500-746">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="5c500-747">Excel, PowerPoint и Word</span><span class="sxs-lookup"><span data-stu-id="5c500-747">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'setSelectedDataAsync(data, callback)'
    fullName: 'setSelectedDataAsync(data, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="5c500-748">Данные, которые необходимо задать.</span><span class="sxs-lookup"><span data-stu-id="5c500-748">The data to be set.</span></span> <span data-ttu-id="5c500-749">Строка или значение [Office. CoercionType](xref:office.Office.CoercionType) , 2D-массив или объект TableData.</span><span class="sxs-lookup"><span data-stu-id="5c500-749">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="5c500-750">Если значение передано `data` :</span><span class="sxs-lookup"><span data-stu-id="5c500-750">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="5c500-751">Строка. Записывается обычный текст или другие данные, которые могут быть приведены к типу string.</span><span class="sxs-lookup"><span data-stu-id="5c500-751">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="5c500-752">В Excel можно также указать данные в виде допустимой формулы, чтобы добавить эту формулу в выбранную ячейку.</span><span class="sxs-lookup"><span data-stu-id="5c500-752">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="5c500-753">Например, если для data указать "=SUM(A1:A5)", будут получены итоговые значения в указанном диапазоне.</span><span class="sxs-lookup"><span data-stu-id="5c500-753">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="5c500-754">Если задать формулу в связанной ячейке, добавленную (или существующую) формулу в этой ячейке будет невозможно считать.</span><span class="sxs-lookup"><span data-stu-id="5c500-754">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="5c500-755">При вызове метода Document.getSelectedDataAsync в выделенной ячейке для считывания ее данных этот метод может возвращать только данные, отображаемые в ячейке (результат формулы).</span><span class="sxs-lookup"><span data-stu-id="5c500-755">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="5c500-756">Массив массивов ("matrix"). Будут вставлены табличные данные без заголовков.</span><span class="sxs-lookup"><span data-stu-id="5c500-756">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="5c500-757">Например, чтобы записать данные в три строки в двух столбцах, можно \[ \[передать массив следующим образом: "R1C1", "\]R1C2", \["R2C1", "R2C2"\], \["R3C1", "R3C2".\]\]</span><span class="sxs-lookup"><span data-stu-id="5c500-757">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="5c500-758">Чтобы записать один столбец из трех строк, передайте массив следующим образом: \[ \["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="5c500-758">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="5c500-759">В Excel вы также можете указать data как массив массивов, содержащих допустимые формулы, чтобы добавить их в выбранные ячейки.</span><span class="sxs-lookup"><span data-stu-id="5c500-759">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="5c500-760">Например, если никакие другие данные не будут перезаписаны, то для \[ \[параметра data — "= Sum (a1: A5)", "= СРЗНАЧ (a1:\] \] A5)" будут добавлены эти две формулы к выделенному фрагменту.</span><span class="sxs-lookup"><span data-stu-id="5c500-760">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="5c500-761">Как и при указании формулы в одной ячейке в текстовом виде, добавленные (или существующие) формулы невозможно считывать после того, как они заданы. Вы можете считывать только результаты формул.</span><span class="sxs-lookup"><span data-stu-id="5c500-761">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="5c500-762">Объект TableData Вставляются табличные данные с заголовками.</span><span class="sxs-lookup"><span data-stu-id="5c500-762">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="5c500-763">Если вы укажете в Excel формулы в объекте TableData, который вы передаете для параметра data, вы можете не получить ожидаемые результаты, потому что функция "вычисляемые столбцы" Excel автоматически копирует формулы в столбце.</span><span class="sxs-lookup"><span data-stu-id="5c500-763">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="5c500-764">Чтобы обойти эту проблему, если вы хотите `data` записать формулы в выбранную таблицу, попробуйте указать данные в виде массива массивов (вместо объекта TableData) и укажите CoercionType как Microsoft. Office. Matrix или "Matrix".</span><span class="sxs-lookup"><span data-stu-id="5c500-764">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="5c500-765">Однако этот метод блокирует функцию "вычисляемые столбцы" только при выполнении одного из следующих условий: (1) вы пишете во все ячейки столбца или (2) в столбце уже есть по крайней мере две разные формулы.</span><span class="sxs-lookup"><span data-stu-id="5c500-765">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: callback
          description: '<span data-ttu-id="5c500-766">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-766">Optional.</span></span> <span data-ttu-id="5c500-767">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-767">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-768">.</span><span class="sxs-lookup"><span data-stu-id="5c500-768"></span></span> <span data-ttu-id="5c500-769">Свойство AsyncResult. Value всегда возвращает значение undefine, так как нет объекта или данных для извлечения.</span><span class="sxs-lookup"><span data-stu-id="5c500-769">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync
    summary: |-
      <span data-ttu-id="5c500-770">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-770">Project documents only.</span></span> <span data-ttu-id="5c500-771">Задание поля задачи для указанного идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-771">Set task field for specified task Id.</span></span>

      <span data-ttu-id="5c500-772">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-772">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="5c500-773">Строка или значение идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-773">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-774">Поля задач.</span><span class="sxs-lookup"><span data-stu-id="5c500-774">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="5c500-775">Значение целевого поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-775">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="5c500-776">Предоставляет параметр для сохранения данных контекста любого типа без изменений для использования при обратном вызове.</span><span class="sxs-lookup"><span data-stu-id="5c500-776">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="5c500-777">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-777">Optional.</span></span> <span data-ttu-id="5c500-778">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-778">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-779">.</span><span class="sxs-lookup"><span data-stu-id="5c500-779"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync_1
    summary: |-
      <span data-ttu-id="5c500-780">Только документы Project.</span><span class="sxs-lookup"><span data-stu-id="5c500-780">Project documents only.</span></span> <span data-ttu-id="5c500-781">Задание поля задачи для указанного идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-781">Set task field for specified task Id.</span></span>

      <span data-ttu-id="5c500-782">**Важно!** этот API работает только в Project 2016 на настольном компьютере с Windows.</span><span class="sxs-lookup"><span data-stu-id="5c500-782">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-783">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-783">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's
          // currently selected in a task view. Then it sets two task field values by calling
          // setTaskFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the
          // addHandlerAsync method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function setTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          setTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected task.
              function setTaskFields(taskGuid) {
                  var targetFields = [Office.ProjectTaskFields.Active, Office.ProjectTaskFields.Notes];
                  var fieldValues = [true, 'Notes for the task.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setTaskFieldAsync(
                          taskGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="5c500-784">Строка или значение идентификатора задачи.</span><span class="sxs-lookup"><span data-stu-id="5c500-784">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="5c500-785">Поля задач.</span><span class="sxs-lookup"><span data-stu-id="5c500-785">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="5c500-786">Значение целевого поля.</span><span class="sxs-lookup"><span data-stu-id="5c500-786">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="5c500-787">Необязательно.</span><span class="sxs-lookup"><span data-stu-id="5c500-787">Optional.</span></span> <span data-ttu-id="5c500-788">Функция, вызываемая при возвращении обратного вызова, единственный параметр которой имеет тип [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="5c500-788">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="5c500-789">.</span><span class="sxs-lookup"><span data-stu-id="5c500-789"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.bindings
    summary: '<span data-ttu-id="5c500-790">Получает объект, предоставляющий доступ к привязкам, определенным в документе.</span><span class="sxs-lookup"><span data-stu-id="5c500-790">Gets an object that provides access to the bindings defined in the document.</span></span>'
    remarks: |-
      <span data-ttu-id="5c500-791">Вы не создаете экземпляр объекта Document непосредственно в вашем скрипте.</span><span class="sxs-lookup"><span data-stu-id="5c500-791">You don't instantiate the Document object directly in your script.</span></span> <span data-ttu-id="5c500-792">Чтобы вызвать элементы объекта Document для взаимодействия с текущим документом или листом, используйте объект `Office.context.document`.</span><span class="sxs-lookup"><span data-stu-id="5c500-792">To call members of the Document object to interact with the current document or worksheet, use `Office.context.document` in your script.</span></span>

      #### <a name="examples"></a><span data-ttu-id="5c500-793">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-793">Examples</span></span>

      ```javascript
      function displayAllBindings() {
          Office.context.document.bindings.getAllAsync(function (asyncResult) {
              var bindingString = '';
              for (var i in asyncResult.value) {
                  bindingString += asyncResult.value[i].id + '\n';
              }
              write('Existing bindings: ' + bindingString);
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
    name: bindings
    fullName: bindings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'bindings: Bindings;'
      return:
        type:
          - office.Office.Bindings
  - uid: office.Office.Document.customXmlParts
    summary: '<span data-ttu-id="5c500-794">Получает объект, представляющий настраиваемые XML-части в документе.</span><span class="sxs-lookup"><span data-stu-id="5c500-794">Gets an object that represents the custom XML parts in the document.</span></span>'
    name: customXmlParts
    fullName: customXmlParts
    langs:
      - typeScript
    type: property
    syntax:
      content: 'customXmlParts: CustomXmlParts;'
      return:
        type:
          - office.Office.CustomXmlParts
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-795">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-795">Examples</span></span>

          ```javascript
          function getCustomXmlParts(){
              Office.context.document.customXmlParts.getByNamespaceAsync('http://tempuri.org', function (asyncResult) {
                  write('Retrieved ' + asyncResult.value.length + ' custom XML parts');
              });
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.Document.mode
    summary: '<span data-ttu-id="5c500-796">Получает режим, в котором находится документ.</span><span class="sxs-lookup"><span data-stu-id="5c500-796">Gets the mode the document is in.</span></span>'
    name: mode
    fullName: mode
    langs:
      - typeScript
    type: property
    syntax:
      content: 'mode: DocumentMode;'
      return:
        type:
          - office.Office.DocumentMode
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-797">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-797">Examples</span></span>

          ```javascript
          function displayDocumentMode() {
              write(Office.context.document.mode);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
          ```javascript
          // The following example initializes the add-in and then gets properties of the
          // Document object that are available in the context of a Project document.
          // A Project document is the opened, active project. To access members of the
          // ProjectDocument object, use the Office.context.document object as shown in
          // the code examples for ProjectDocument methods and events.
          // The example assumes your add-in has a reference to the jQuery library and
          // that the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information about the document.
                      showDocumentProperties();
                  });
              };

              // Get the document mode and the URL of the active project.
              function showDocumentProperties() {
                  var output = String.format(
                      'The document mode is {0}.<br/>The URL of the active project is {1}.',
                      Office.context.document.mode,
                      Office.context.document.url);
                  $('#message').html(output);
              }
          })();
          ```
  - uid: office.Office.Document.settings
    summary: '<span data-ttu-id="5c500-798">Получает объект, который представляет сохраненные настраиваемые параметры надстройки области задач или контентной надстройки для текущего документа.</span><span class="sxs-lookup"><span data-stu-id="5c500-798">Gets an object that represents the saved custom settings of the content or task pane add-in for the current document.</span></span>'
    name: settings
    fullName: settings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'settings: Settings;'
      return:
        type:
          - office.Office.Settings
  - uid: office.Office.Document.url
    summary: '<span data-ttu-id="5c500-799">Получает URL-адрес документа, открытого ведущим приложением.</span><span class="sxs-lookup"><span data-stu-id="5c500-799">Gets the URL of the document that the host application currently has open.</span></span> <span data-ttu-id="5c500-800">Возвращает значение null, если URL-адрес недоступен.</span><span class="sxs-lookup"><span data-stu-id="5c500-800">Returns null if the URL is unavailable.</span></span>'
    name: url
    fullName: url
    langs:
      - typeScript
    type: property
    syntax:
      content: 'url: string;'
      return:
        type:
          - string
        description: |-


          #### <a name="examples"></a><span data-ttu-id="5c500-801">Примеры</span><span class="sxs-lookup"><span data-stu-id="5c500-801">Examples</span></span>

          ```javascript
          function displayDocumentUrl() {
              write(Office.context.document.url);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```